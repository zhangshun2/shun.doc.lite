# 4.1.12 二叉树专题(16-17题)-Java版本

# 4.1.12 二叉树专题(16-17题) - Java版本
> 本文档包含LeetCode热题100中二叉树专题的最后2道题目，提供基础解法和进阶优化版本
>

---

## 📊 题目概览
| 题号 | 题目 | 难度 | 标签 |
| --- | --- | --- | --- |
| 297 | 二叉树的序列化与反序列化 | 🔴 困难 | 树, 深度优先搜索, 广度优先搜索, 设计, 字符串, 二叉树 |
| 208 | 实现 Trie (前缀树) | 🟡 中等 | 设计, 字典树, 哈希表, 字符串 |


---

## 1️⃣ 二叉树的序列化与反序列化 (LeetCode 297)
### 📝 题目描述
序列化是将一个数据结构或者对象转换为连续的比特位的操作，进而可以将转换后的数据存储在一个文件或者内存中，同时也可以通过网络传输到另一个计算机环境，采取相反方式重构得到原数据。

请设计一个算法来实现二叉树的序列化与反序列化。这里不限定你的序列化/反序列化算法执行逻辑，你只需要保证一个二叉树可以被序列化为一个字符串并且将这个字符串反序列化为原始的树结构。

**示例 1：**

```plain
输入：root = [1,2,3,null,null,4,5]
输出：[1,2,3,null,null,4,5]
```

**示例 2：**

```plain
输入：root = []
输出：[]
```

### 🔧 基础解法：前序遍历
```java
public class Codec {
    private static final String NULL_NODE = "null";
    private static final String DELIMITER = ",";
    
    // 序列化：将二叉树转换为字符串
    public String serialize(TreeNode root) {
        StringBuilder sb = new StringBuilder();
        serializeHelper(root, sb);
        return sb.toString();
    }
    
    private void serializeHelper(TreeNode node, StringBuilder sb) {
        if (node == null) {
            sb.append(NULL_NODE).append(DELIMITER);
            return;
        }
        
        // 前序遍历：根 -> 左 -> 右
        sb.append(node.val).append(DELIMITER);
        serializeHelper(node.left, sb);
        serializeHelper(node.right, sb);
    }
    
    // 反序列化：将字符串转换为二叉树
    public TreeNode deserialize(String data) {
        String[] nodes = data.split(DELIMITER);
        Queue<String> queue = new LinkedList<>(Arrays.asList(nodes));
        return deserializeHelper(queue);
    }
    
    private TreeNode deserializeHelper(Queue<String> queue) {
        String val = queue.poll();
        
        if (NULL_NODE.equals(val)) {
            return null;
        }
        
        // 构建当前节点
        TreeNode node = new TreeNode(Integer.parseInt(val));
        
        // 递归构建左右子树
        node.left = deserializeHelper(queue);
        node.right = deserializeHelper(queue);
        
        return node;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)，每个节点访问一次
+ 空间复杂度：O(n)，递归调用栈和存储空间

### 🚀 进阶解法：层序遍历
```java
public class Codec {
    private static final String NULL_NODE = "null";
    private static final String DELIMITER = ",";
    
    // 序列化：层序遍历
    public String serialize(TreeNode root) {
        if (root == null) {
            return "";
        }
        
        StringBuilder sb = new StringBuilder();
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        
        while (!queue.isEmpty()) {
            TreeNode node = queue.poll();
            
            if (node == null) {
                sb.append(NULL_NODE).append(DELIMITER);
            } else {
                sb.append(node.val).append(DELIMITER);
                queue.offer(node.left);
                queue.offer(node.right);
            }
        }
        
        return sb.toString();
    }
    
    // 反序列化：层序遍历
    public TreeNode deserialize(String data) {
        if (data.isEmpty()) {
            return null;
        }
        
        String[] nodes = data.split(DELIMITER);
        TreeNode root = new TreeNode(Integer.parseInt(nodes[0]));
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        
        int index = 1;
        while (!queue.isEmpty() && index < nodes.length) {
            TreeNode node = queue.poll();
            
            // 处理左子节点
            if (!NULL_NODE.equals(nodes[index])) {
                node.left = new TreeNode(Integer.parseInt(nodes[index]));
                queue.offer(node.left);
            }
            index++;
            
            // 处理右子节点
            if (index < nodes.length && !NULL_NODE.equals(nodes[index])) {
                node.right = new TreeNode(Integer.parseInt(nodes[index]));
                queue.offer(node.right);
            }
            index++;
        }
        
        return root;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 最优解法：后序遍历（紧凑格式）
```java
public class Codec {
    private int index = 0;
    
    // 序列化：后序遍历
    public String serialize(TreeNode root) {
        StringBuilder sb = new StringBuilder();
        postorderSerialize(root, sb);
        return sb.toString();
    }
    
    private void postorderSerialize(TreeNode node, StringBuilder sb) {
        if (node == null) {
            sb.append("#,");
            return;
        }
        
        postorderSerialize(node.left, sb);
        postorderSerialize(node.right, sb);
        sb.append(node.val).append(",");
    }
    
    // 反序列化：逆后序遍历
    public TreeNode deserialize(String data) {
        String[] nodes = data.split(",");
        index = nodes.length - 1;
        return postorderDeserialize(nodes);
    }
    
    private TreeNode postorderDeserialize(String[] nodes) {
        String val = nodes[index--];
        
        if ("#".equals(val)) {
            return null;
        }
        
        TreeNode node = new TreeNode(Integer.parseInt(val));
        // 注意：后序遍历的逆序是 根 -> 右 -> 左
        node.right = postorderDeserialize(nodes);
        node.left = postorderDeserialize(nodes);
        
        return node;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)，递归调用栈

---

## 2️⃣ 实现 Trie (前缀树) (LeetCode 208)
### 📝 题目描述
Trie（发音类似 "try"）或者说前缀树是一种树形数据结构，用于高效地存储和检索字符串数据集中的键。这一数据结构有相当多的应用情景，例如自动补完和拼写检查。

请你实现 Trie 类：

+ `Trie()` 初始化前缀树对象。
+ `void insert(String word)` 向前缀树中插入字符串 `word`。
+ `boolean search(String word)` 如果字符串 `word` 在前缀树中，返回 `true`；否则，返回 `false`。
+ `boolean startsWith(String prefix)` 如果之前已经插入的字符串 `word` 的前缀之一为 `prefix`，返回 `true`；否则，返回 `false`。

**示例：**

```plain
输入
["Trie", "insert", "search", "search", "startsWith", "insert", "search"]
[[], ["apple"], ["apple"], ["app"], ["app"], ["app"], ["app"]]
输出
[null, null, true, false, true, null, true]

解释
Trie trie = new Trie();
trie.insert("apple");
trie.search("apple");   // 返回 True
trie.search("app");     // 返回 False
trie.startsWith("app"); // 返回 True
trie.insert("app");
trie.search("app");     // 返回 True
```

### 🔧 基础解法：数组实现
```java
class Trie {
    private TrieNode root;
    
    // Trie节点定义
    class TrieNode {
        TrieNode[] children;
        boolean isEnd;
        
        public TrieNode() {
            children = new TrieNode[26]; // 26个小写字母
            isEnd = false;
        }
    }
    
    public Trie() {
        root = new TrieNode();
    }
    
    // 插入单词
    public void insert(String word) {
        TrieNode current = root;
        
        for (char c : word.toCharArray()) {
            int index = c - 'a';
            
            if (current.children[index] == null) {
                current.children[index] = new TrieNode();
            }
            
            current = current.children[index];
        }
        
        current.isEnd = true;
    }
    
    // 搜索单词
    public boolean search(String word) {
        TrieNode node = searchPrefix(word);
        return node != null && node.isEnd;
    }
    
    // 检查前缀
    public boolean startsWith(String prefix) {
        return searchPrefix(prefix) != null;
    }
    
    // 搜索前缀的辅助方法
    private TrieNode searchPrefix(String prefix) {
        TrieNode current = root;
        
        for (char c : prefix.toCharArray()) {
            int index = c - 'a';
            
            if (current.children[index] == null) {
                return null;
            }
            
            current = current.children[index];
        }
        
        return current;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(m)，其中m是字符串长度
+ 空间复杂度：O(ALPHABET_SIZE * N * M)，最坏情况下的空间复杂度

### 🚀 进阶解法：哈希表实现
```java
class Trie {
    private TrieNode root;
    
    // Trie节点定义（使用哈希表）
    class TrieNode {
        Map<Character, TrieNode> children;
        boolean isEnd;
        
        public TrieNode() {
            children = new HashMap<>();
            isEnd = false;
        }
    }
    
    public Trie() {
        root = new TrieNode();
    }
    
    public void insert(String word) {
        TrieNode current = root;
        
        for (char c : word.toCharArray()) {
            current.children.putIfAbsent(c, new TrieNode());
            current = current.children.get(c);
        }
        
        current.isEnd = true;
    }
    
    public boolean search(String word) {
        TrieNode node = searchPrefix(word);
        return node != null && node.isEnd;
    }
    
    public boolean startsWith(String prefix) {
        return searchPrefix(prefix) != null;
    }
    
    private TrieNode searchPrefix(String prefix) {
        TrieNode current = root;
        
        for (char c : prefix.toCharArray()) {
            if (!current.children.containsKey(c)) {
                return null;
            }
            current = current.children.get(c);
        }
        
        return current;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(m)
+ 空间复杂度：O(N * M)，更节省空间

### 🚀 最优解法：压缩Trie（Radix Tree）
```java
class Trie {
    private TrieNode root;
    
    // 压缩Trie节点
    class TrieNode {
        Map<String, TrieNode> children;
        boolean isEnd;
        
        public TrieNode() {
            children = new HashMap<>();
            isEnd = false;
        }
    }
    
    public Trie() {
        root = new TrieNode();
    }
    
    public void insert(String word) {
        insertHelper(root, word, 0);
    }
    
    private void insertHelper(TrieNode node, String word, int index) {
        if (index == word.length()) {
            node.isEnd = true;
            return;
        }
        
        String suffix = word.substring(index);
        
        // 查找匹配的边
        for (String edge : node.children.keySet()) {
            String commonPrefix = getCommonPrefix(edge, suffix);
            
            if (!commonPrefix.isEmpty()) {
                if (commonPrefix.equals(edge)) {
                    // 完全匹配，继续向下
                    insertHelper(node.children.get(edge), word, index + edge.length());
                    return;
                } else {
                    // 部分匹配，需要分裂节点
                    TrieNode oldChild = node.children.remove(edge);
                    TrieNode newChild = new TrieNode();
                    
                    // 创建新的中间节点
                    node.children.put(commonPrefix, newChild);
                    newChild.children.put(edge.substring(commonPrefix.length()), oldChild);
                    
                    // 继续插入剩余部分
                    insertHelper(newChild, word, index + commonPrefix.length());
                    return;
                }
            }
        }
        
        // 没有匹配的边，创建新边
        TrieNode newNode = new TrieNode();
        newNode.isEnd = true;
        node.children.put(suffix, newNode);
    }
    
    public boolean search(String word) {
        TrieNode node = searchHelper(word);
        return node != null && node.isEnd;
    }
    
    public boolean startsWith(String prefix) {
        return searchHelper(prefix) != null;
    }
    
    private TrieNode searchHelper(String word) {
        TrieNode current = root;
        int index = 0;
        
        while (index < word.length()) {
            String suffix = word.substring(index);
            boolean found = false;
            
            for (String edge : current.children.keySet()) {
                if (suffix.startsWith(edge)) {
                    current = current.children.get(edge);
                    index += edge.length();
                    found = true;
                    break;
                } else if (edge.startsWith(suffix)) {
                    // 前缀匹配
                    return current.children.get(edge);
                }
            }
            
            if (!found) {
                return null;
            }
        }
        
        return current;
    }
    
    private String getCommonPrefix(String s1, String s2) {
        int i = 0;
        while (i < s1.length() && i < s2.length() && s1.charAt(i) == s2.charAt(i)) {
            i++;
        }
        return s1.substring(0, i);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(m)，但常数因子更小
+ 空间复杂度：O(N)，大幅节省空间

**解题思路：**

**Trie树的核心思想：**

1. **共享前缀**：相同前缀的字符串共享路径
2. **逐字符构建**：每个节点代表一个字符
3. **标记结束**：用isEnd标记完整单词的结束

**实现方式对比：**

1. **数组实现**：适合字符集固定且较小的情况
2. **哈希表实现**：适合字符集较大或不确定的情况
3. **压缩Trie**：适合需要节省空间的情况

**应用场景：**

+ 自动补全
+ 拼写检查
+ IP路由
+ 字符串匹配

---

## 📚 总结
### 🎯 核心知识点
1. **序列化与反序列化**：数据结构的持久化和恢复
2. **多种遍历方式**：前序、中序、后序、层序的应用
3. **Trie树结构**：前缀树的设计和实现
4. **空间优化技巧**：压缩存储、共享结构
5. **字符串处理**：分割、解析、编码

### 💡 解题模式
+ **序列化模式**：选择合适的遍历方式进行编码
+ **反序列化模式**：根据编码方式选择对应的解码策略
+ **Trie设计模式**：根据应用场景选择合适的实现方式
+ **空间优化思想**：压缩、共享、延迟创建
+ **递归与迭代**：根据问题特点选择实现方式

### 🔄 刷题建议
1. **掌握序列化技巧**：理解不同遍历方式的编码特点
2. **熟悉Trie应用**：自动补全、字符串匹配等场景
3. **练习空间优化**：学会在时间和空间之间做权衡
4. **理解数据结构设计**：根据需求选择合适的实现方式
5. **培养系统设计思维**：考虑实际应用中的性能和扩展性

### 🎯 相关题目
**序列化相关：**

+ **LeetCode 449**：序列化和反序列化二叉搜索树
+ **LeetCode 428**：序列化和反序列化N叉树

**Trie相关：**

+ **LeetCode 211**：添加与搜索单词 - 数据结构设计
+ **LeetCode 212**：单词搜索 II
+ **LeetCode 648**：单词替换

---

## 🎉 二叉树专题完结
恭喜你完成了LeetCode热题100中所有17道二叉树题目的学习！

### 📈 学习成果
+ ✅ 掌握了二叉树的各种遍历方式：前序、中序、后序、层序
+ ✅ 学会了递归和迭代两种实现方式
+ ✅ 理解了二叉搜索树的性质和应用
+ ✅ 熟悉了树的构造、变形、路径问题
+ ✅ 掌握了复杂树问题的解决策略
+ ✅ 学会了Trie树的设计和实现

### 🚀 下一步学习
建议继续学习：

+ **动态规划专题**：状态转移和优化策略
+ **搜索与回溯专题**：DFS和BFS的深入应用
+ **栈专题**：栈的应用和单调栈

### 💪 二叉树核心技能总结
1. **遍历算法**：四种遍历方式的递归和迭代实现
2. **递归思维**：分治、后序处理、全局状态维护
3. **BST性质**：中序遍历有序性的应用
4. **路径问题**：前缀和、回溯、最值计算
5. **树的构造**：根据遍历序列重建树
6. **空间优化**：Morris遍历、压缩存储
7. **数据结构设计**：Trie树等专用结构

---

**上一篇：** [4.1.11 二叉树专题(11-15题) - Java版本](./4.1.11%20二叉树专题(11-15题)-Java版本.md)  
**下一篇：** [4.1.13 动态规划专题(1-5题) - Java版本](./4.1.13%20动态规划专题(1-5题)-Java版本.md)



> 更新: 2025-08-25 19:00:24  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/6e00845df0e895c9eabf98b2d6724921>