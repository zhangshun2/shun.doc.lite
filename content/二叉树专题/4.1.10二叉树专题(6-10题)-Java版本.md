# 4.1.10 二叉树专题(6-10题)-Java版本

# 4.1.10 二叉树专题(6-10题) - Java版本
> 本文档包含LeetCode热题100中二叉树专题的第6-10道题目，提供基础解法和进阶优化版本
>

---

## 📊 题目概览
| 题号 | 题目 | 难度 | 标签 |
| --- | --- | --- | --- |
| 102 | 二叉树的层序遍历 | 🟡 中等 | 树, 广度优先搜索, 二叉树 |
| 108 | 将有序数组转换为二叉搜索树 | 🟢 简单 | 树, 二叉搜索树, 数组, 分治, 二叉树 |
| 98 | 验证二叉搜索树 | 🟡 中等 | 树, 深度优先搜索, 二叉搜索树, 二叉树 |
| 230 | 二叉搜索树中第K小的元素 | 🟡 中等 | 树, 深度优先搜索, 二叉搜索树, 二叉树 |
| 199 | 二叉树的右视图 | 🟡 中等 | 树, 深度优先搜索, 广度优先搜索, 二叉树 |


---

## 1️⃣ 二叉树的层序遍历 (LeetCode 102)
### 📝 题目描述
给你一个二叉树，请你返回其按层序遍历得到的节点值。（即逐层地，从左到右访问所有节点）。

**示例：**

```plain
二叉树：[3,9,20,null,null,15,7]

    3
   / \
  9  20
    /  \
   15   7

返回其层序遍历结果：
[
  [3],
  [9,20],
  [15,7]
]
```

### 🔧 基础解法：BFS + 队列
```java
class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        if (root == null) {
            return result;
        }
        
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        
        while (!queue.isEmpty()) {
            int size = queue.size();
            List<Integer> level = new ArrayList<>();
            
            // 处理当前层的所有节点
            for (int i = 0; i < size; i++) {
                TreeNode node = queue.poll();
                level.add(node.val);
                
                // 将下一层节点加入队列
                if (node.left != null) {
                    queue.offer(node.left);
                }
                if (node.right != null) {
                    queue.offer(node.right);
                }
            }
            
            result.add(level);
        }
        
        return result;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)，每个节点访问一次
+ 空间复杂度：O(w)，其中w是树的最大宽度

### 🚀 进阶解法：DFS + 递归
```java
class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        dfs(root, 0, result);
        return result;
    }
    
    private void dfs(TreeNode node, int level, List<List<Integer>> result) {
        if (node == null) {
            return;
        }
        
        // 如果当前层还没有列表，创建一个新的
        if (level >= result.size()) {
            result.add(new ArrayList<>());
        }
        
        // 将当前节点值加入对应层
        result.get(level).add(node.val);
        
        // 递归处理左右子树
        dfs(node.left, level + 1, result);
        dfs(node.right, level + 1, result);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)，递归调用栈

---

## 2️⃣ 将有序数组转换为二叉搜索树 (LeetCode 108)
### 📝 题目描述
将一个按照升序排列的有序数组，转换为一棵高度平衡的二叉搜索树。

本题中，一个高度平衡二叉树是指一个二叉树每个节点的左右两个子树的高度差的绝对值不超过 1。

**示例：**

```plain
给定有序数组: [-10,-3,0,5,9]

一个可能的答案是：[0,-3,9,-10,null,5]，它可以表示下面这个高度平衡二叉搜索树：

      0
     / \
   -3   9
   /   /
 -10  5
```

### 🔧 基础解法：递归 + 分治
```java
class Solution {
    public TreeNode sortedArrayToBST(int[] nums) {
        return buildBST(nums, 0, nums.length - 1);
    }
    
    private TreeNode buildBST(int[] nums, int left, int right) {
        if (left > right) {
            return null;
        }
        
        // 选择中间元素作为根节点
        int mid = left + (right - left) / 2;
        TreeNode root = new TreeNode(nums[mid]);
        
        // 递归构建左右子树
        root.left = buildBST(nums, left, mid - 1);
        root.right = buildBST(nums, mid + 1, right);
        
        return root;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)，每个元素访问一次
+ 空间复杂度：O(log n)，递归调用栈

### 🚀 进阶解法：迭代 + 队列
```java
class Solution {
    public TreeNode sortedArrayToBST(int[] nums) {
        if (nums.length == 0) {
            return null;
        }
        
        // 使用队列存储节点和对应的数组范围
        Queue<TreeNode> nodeQueue = new LinkedList<>();
        Queue<int[]> rangeQueue = new LinkedList<>();
        
        int mid = nums.length / 2;
        TreeNode root = new TreeNode(nums[mid]);
        nodeQueue.offer(root);
        rangeQueue.offer(new int[]{0, nums.length - 1});
        
        while (!nodeQueue.isEmpty()) {
            TreeNode node = nodeQueue.poll();
            int[] range = rangeQueue.poll();
            int left = range[0], right = range[1];
            int nodeMid = left + (right - left) / 2;
            
            // 构建左子树
            if (left < nodeMid) {
                int leftMid = left + (nodeMid - 1 - left) / 2;
                node.left = new TreeNode(nums[leftMid]);
                nodeQueue.offer(node.left);
                rangeQueue.offer(new int[]{left, nodeMid - 1});
            }
            
            // 构建右子树
            if (nodeMid < right) {
                int rightMid = (nodeMid + 1) + (right - (nodeMid + 1)) / 2;
                node.right = new TreeNode(nums[rightMid]);
                nodeQueue.offer(node.right);
                rangeQueue.offer(new int[]{nodeMid + 1, right});
            }
        }
        
        return root;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)，队列空间

---

## 3️⃣ 验证二叉搜索树 (LeetCode 98)
### 📝 题目描述
给定一个二叉树，判断其是否是一个有效的二叉搜索树。

假设一个二叉搜索树具有如下特征：

+ 节点的左子树只包含小于当前节点的数。
+ 节点的右子树只包含大于当前节点的数。
+ 所有左子树和右子树自身必须也是二叉搜索树。

**示例 1：**

```plain
输入:
    2
   / \
  1   3
输出: true
```

**示例 2：**

```plain
输入:
    5
   / \
  1   4
     / \
    3   6
输出: false
解释: 输入为: [5,1,4,null,null,3,6]。
     根节点的值为 5 ，但是其右子节点值为 4 。
```

### 🔧 基础解法：递归 + 边界检查
```java
class Solution {
    public boolean isValidBST(TreeNode root) {
        return validate(root, null, null);
    }
    
    private boolean validate(TreeNode node, Integer min, Integer max) {
        if (node == null) {
            return true;
        }
        
        // 检查当前节点是否违反BST性质
        if ((min != null && node.val <= min) || 
            (max != null && node.val >= max)) {
            return false;
        }
        
        // 递归验证左右子树
        return validate(node.left, min, node.val) && 
               validate(node.right, node.val, max);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)，递归调用栈

### 🚀 进阶解法：中序遍历
```java
class Solution {
    private Integer prev = null;
    
    public boolean isValidBST(TreeNode root) {
        return inorder(root);
    }
    
    private boolean inorder(TreeNode node) {
        if (node == null) {
            return true;
        }
        
        // 遍历左子树
        if (!inorder(node.left)) {
            return false;
        }
        
        // 检查当前节点
        if (prev != null && node.val <= prev) {
            return false;
        }
        prev = node.val;
        
        // 遍历右子树
        return inorder(node.right);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)

### 🚀 最优解法：迭代中序遍历
```java
class Solution {
    public boolean isValidBST(TreeNode root) {
        Stack<TreeNode> stack = new Stack<>();
        TreeNode current = root;
        Integer prev = null;
        
        while (current != null || !stack.isEmpty()) {
            // 一直向左走到底
            while (current != null) {
                stack.push(current);
                current = current.left;
            }
            
            // 弹出栈顶元素
            current = stack.pop();
            
            // 检查BST性质
            if (prev != null && current.val <= prev) {
                return false;
            }
            prev = current.val;
            
            // 转向右子树
            current = current.right;
        }
        
        return true;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)

---

## 4️⃣ 二叉搜索树中第K小的元素 (LeetCode 230)
### 📝 题目描述
给定一个二叉搜索树的根节点 `root`，和一个整数 `k`，请你设计一个算法查找其中第 `k` 个最小的元素（从 1 开始计数）。

**示例 1：**

```plain
输入：root = [3,1,4,null,2], k = 1
输出：1
```

**示例 2：**

```plain
输入：root = [5,3,6,2,4,null,null,1], k = 3
输出：3
```

### 🔧 基础解法：中序遍历 + 计数
```java
class Solution {
    private int count = 0;
    private int result = 0;
    
    public int kthSmallest(TreeNode root, int k) {
        inorder(root, k);
        return result;
    }
    
    private void inorder(TreeNode node, int k) {
        if (node == null || count >= k) {
            return;
        }
        
        // 遍历左子树
        inorder(node.left, k);
        
        // 访问当前节点
        count++;
        if (count == k) {
            result = node.val;
            return;
        }
        
        // 遍历右子树
        inorder(node.right, k);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(h + k)，其中h是树的高度
+ 空间复杂度：O(h)，递归调用栈

### 🚀 进阶解法：迭代中序遍历
```java
class Solution {
    public int kthSmallest(TreeNode root, int k) {
        Stack<TreeNode> stack = new Stack<>();
        TreeNode current = root;
        int count = 0;
        
        while (current != null || !stack.isEmpty()) {
            // 一直向左走到底
            while (current != null) {
                stack.push(current);
                current = current.left;
            }
            
            // 弹出栈顶元素
            current = stack.pop();
            count++;
            
            // 找到第k小的元素
            if (count == k) {
                return current.val;
            }
            
            // 转向右子树
            current = current.right;
        }
        
        return -1; // 不会到达这里
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(h + k)
+ 空间复杂度：O(h)

### 🚀 最优解法：Morris遍历（O(1)空间）
```java
class Solution {
    public int kthSmallest(TreeNode root, int k) {
        TreeNode current = root;
        int count = 0;
        
        while (current != null) {
            if (current.left == null) {
                // 访问当前节点
                count++;
                if (count == k) {
                    return current.val;
                }
                current = current.right;
            } else {
                // 找到前驱节点
                TreeNode predecessor = current.left;
                while (predecessor.right != null && predecessor.right != current) {
                    predecessor = predecessor.right;
                }
                
                if (predecessor.right == null) {
                    // 建立线索
                    predecessor.right = current;
                    current = current.left;
                } else {
                    // 恢复树结构，访问当前节点
                    predecessor.right = null;
                    count++;
                    if (count == k) {
                        return current.val;
                    }
                    current = current.right;
                }
            }
        }
        
        return -1;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(h + k)
+ 空间复杂度：O(1)

---

## 5️⃣ 二叉树的右视图 (LeetCode 199)
### 📝 题目描述
给定一棵二叉树，想象自己站在它的右侧，按照从顶部到底部的顺序，返回从右侧所能看到的节点值。

**示例：**

```plain
输入: [1,2,3,null,5,null,4]
输出: [1,3,4]
解释:

   1            <---
 /   \
2     3         <---
 \     \
  5     4       <---
```

### 🔧 基础解法：层序遍历
```java
class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        if (root == null) {
            return result;
        }
        
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        
        while (!queue.isEmpty()) {
            int size = queue.size();
            
            for (int i = 0; i < size; i++) {
                TreeNode node = queue.poll();
                
                // 每层的最后一个节点就是右视图能看到的
                if (i == size - 1) {
                    result.add(node.val);
                }
                
                if (node.left != null) {
                    queue.offer(node.left);
                }
                if (node.right != null) {
                    queue.offer(node.right);
                }
            }
        }
        
        return result;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(w)，队列空间

### 🚀 进阶解法：DFS（先右后左）
```java
class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        dfs(root, 0, result);
        return result;
    }
    
    private void dfs(TreeNode node, int level, List<Integer> result) {
        if (node == null) {
            return;
        }
        
        // 如果是该层第一次访问，添加到结果中
        if (level >= result.size()) {
            result.add(node.val);
        }
        
        // 先访问右子树，再访问左子树
        dfs(node.right, level + 1, result);
        dfs(node.left, level + 1, result);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)，递归调用栈

### 🚀 最优解法：优化DFS
```java
class Solution {
    private List<Integer> result = new ArrayList<>();
    
    public List<Integer> rightSideView(TreeNode root) {
        dfs(root, 0);
        return result;
    }
    
    private void dfs(TreeNode node, int level) {
        if (node == null) {
            return;
        }
        
        // 每层第一次访问的节点（从右开始）
        if (level == result.size()) {
            result.add(node.val);
        }
        
        // 优先访问右子树
        dfs(node.right, level + 1);
        dfs(node.left, level + 1);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(h)

**解题思路：**

**方法一：层序遍历**

1. 使用BFS按层遍历二叉树
2. 每层的最后一个节点就是右视图能看到的节点

**方法二：DFS（推荐）**

1. 使用DFS遍历，但优先访问右子树
2. 每层第一次访问到的节点就是右视图能看到的节点
3. 通过level参数控制层数，通过result.size()判断是否是该层第一次访问

---

## 📚 总结
### 🎯 核心知识点
1. **层序遍历**：BFS的经典应用，按层处理节点
2. **二叉搜索树**：中序遍历得到有序序列的性质
3. **分治算法**：递归构建平衡二叉搜索树
4. **边界检查**：验证BST时维护上下界
5. **Morris遍历**：O(1)空间复杂度的树遍历
6. **视图问题**：通过遍历顺序解决视角问题

### 💡 解题模式
+ **BFS模板**：队列 + 层级处理
+ **BST性质利用**：中序遍历的有序性
+ **分治思想**：选择中点构建平衡树
+ **边界维护**：递归过程中传递约束条件
+ **遍历优化**：根据问题特点选择遍历顺序
+ **空间优化**：Morris遍历等高级技巧

### 🔄 刷题建议
1. **掌握BFS模板**：层序遍历是很多树问题的基础
2. **理解BST性质**：中序遍历的有序性是关键
3. **练习分治思想**：递归构建和验证的能力
4. **熟悉边界处理**：递归过程中的约束传递
5. **学习空间优化**：Morris遍历等高级技巧

---

**上一篇：** [4.1.9 二叉树专题(1-5题) - Java版本](./4.1.9%20二叉树专题(1-5题)-Java版本.md)  
**下一篇：** [4.1.11 二叉树专题(11-15题) - Java版本](./4.1.11%20二叉树专题(11-15题)-Java版本.md)



> 更新: 2025-08-25 19:00:21  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/1503e9092e51c4db00e70cc7fb1a645e>