# 二叉树遍历算法详解

# 二叉树遍历算法详解
## 概述
二叉树遍历是树结构中最基本的操作之一。根据访问节点的顺序不同，可以分为三种经典的深度优先遍历方式：前序遍历、中序遍历和后序遍历。每种遍历都可以用递归和迭代（栈）两种方式实现。

## 二叉树节点定义
```java
class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;
    
    TreeNode() {}
    TreeNode(int val) { this.val = val; }
    TreeNode(int val, TreeNode left, TreeNode right) {
        this.val = val;
        this.left = left;
        this.right = right;
    }
}
```

## 前序遍历（Pre-order Traversal）
**遍历顺序**：根节点 → 左子树 → 右子树

### 递归实现
```java
public List<Integer> preorderTraversalRecursive(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    preorderHelper(root, result);
    return result;
}

private void preorderHelper(TreeNode node, List<Integer> result) {
    if (node == null) return;
    
    result.add(node.val);           // 访问根节点
    preorderHelper(node.left, result);   // 遍历左子树
    preorderHelper(node.right, result);  // 遍历右子树
}
```

### 栈实现
```java
public List<Integer> preorderTraversal(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    if (root == null) return result;
    
    Stack<TreeNode> stack = new Stack<>();
    stack.push(root);
    
    while (!stack.isEmpty()) {
        TreeNode node = stack.pop();
        result.add(node.val);  // 访问根节点
        
        // 先压右子树，再压左子树（保证左子树先被访问）
        if (node.right != null) {
            stack.push(node.right);
        }
        if (node.left != null) {
            stack.push(node.left);
        }
    }
    
    return result;
}
```

**应用场景**：

+ 复制二叉树
+ 前缀表达式求值
+ 树的序列化

## 中序遍历（In-order Traversal）
**遍历顺序**：左子树 → 根节点 → 右子树

### 递归实现
```java
public List<Integer> inorderTraversalRecursive(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    inorderHelper(root, result);
    return result;
}

private void inorderHelper(TreeNode node, List<Integer> result) {
    if (node == null) return;
    
    inorderHelper(node.left, result);    // 遍历左子树
    result.add(node.val);               // 访问根节点
    inorderHelper(node.right, result);   // 遍历右子树
}
```

### 栈实现
```java
public List<Integer> inorderTraversal(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    Stack<TreeNode> stack = new Stack<>();
    TreeNode current = root;
    
    while (current != null || !stack.isEmpty()) {
        // 一直向左走，将路径上的节点压栈
        while (current != null) {
            stack.push(current);
            current = current.left;
        }
        
        // 弹出栈顶节点并访问
        current = stack.pop();
        result.add(current.val);
        
        // 转向右子树
        current = current.right;
    }
    
    return result;
}
```

**应用场景**：

+ 二叉搜索树的有序输出
+ 中缀表达式的输出

## 后序遍历（Post-order Traversal）
**遍历顺序**：左子树 → 右子树 → 根节点

### 递归实现
```java
public List<Integer> postorderTraversalRecursive(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    postorderHelper(root, result);
    return result;
}

private void postorderHelper(TreeNode node, List<Integer> result) {
    if (node == null) return;
    
    postorderHelper(node.left, result);   // 遍历左子树
    postorderHelper(node.right, result);  // 遍历右子树
    result.add(node.val);                // 访问根节点
}
```

### 栈实现（方法一：单栈 + 标记）
```java
public List<Integer> postorderTraversal(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    if (root == null) return result;
    
    Stack<TreeNode> stack = new Stack<>();
    TreeNode lastVisited = null;
    TreeNode current = root;
    
    while (current != null || !stack.isEmpty()) {
        if (current != null) {
            // 向左走到底
            stack.push(current);
            current = current.left;
        } else {
            // 查看栈顶节点
            TreeNode peekNode = stack.peek();
            
            // 如果右子树存在且未被访问，转向右子树
            if (peekNode.right != null && lastVisited != peekNode.right) {
                current = peekNode.right;
            } else {
                // 访问节点
                result.add(peekNode.val);
                lastVisited = stack.pop();
            }
        }
    }
    
    return result;
}
```

### 栈实现（方法二：双栈法）
```java
public List<Integer> postorderTraversalTwoStacks(TreeNode root) {
    List<Integer> result = new ArrayList<>();
    if (root == null) return result;
    
    Stack<TreeNode> stack1 = new Stack<>();  // 遍历栈
    Stack<TreeNode> stack2 = new Stack<>();  // 结果栈
    
    stack1.push(root);
    
    while (!stack1.isEmpty()) {
        TreeNode node = stack1.pop();
        stack2.push(node);
        
        // 先压左子树，再压右子树（得到 根→右→左 的顺序）
        if (node.left != null) {
            stack1.push(node.left);
        }
        if (node.right != null) {
            stack1.push(node.right);
        }
    }
    
    // 从第二个栈弹出，得到后序遍历结果
    while (!stack2.isEmpty()) {
        result.add(stack2.pop().val);
    }
    
    return result;
}
```

**应用场景**：

+ 计算目录大小
+ 删除二叉树
+ 后缀表达式求值
+ 计算二叉树的高度

## 层序遍历（Level-order Traversal）
**遍历顺序**：按层从左到右访问节点

```java
public List<List<Integer>> levelOrder(TreeNode root) {
    List<List<Integer>> result = new ArrayList<>();
    if (root == null) return result;
    
    Queue<TreeNode> queue = new LinkedList<>();
    queue.offer(root);
    
    while (!queue.isEmpty()) {
        int levelSize = queue.size();
        List<Integer> currentLevel = new ArrayList<>();
        
        for (int i = 0; i < levelSize; i++) {
            TreeNode node = queue.poll();
            currentLevel.add(node.val);
            
            if (node.left != null) {
                queue.offer(node.left);
            }
            if (node.right != null) {
                queue.offer(node.right);
            }
        }
        
        result.add(currentLevel);
    }
    
    return result;
}
```

## 测试示例
```java
public class BinaryTreeTraversalTest {
    public static void main(String[] args) {
        // 构建测试二叉树
        //       1
        //      / \
        //     2   3
        //    / \
        //   4   5
        TreeNode root = new TreeNode(1);
        root.left = new TreeNode(2);
        root.right = new TreeNode(3);
        root.left.left = new TreeNode(4);
        root.left.right = new TreeNode(5);
        
        System.out.println("前序遍历：" + preorderTraversal(root));  // [1, 2, 4, 5, 3]
        System.out.println("中序遍历：" + inorderTraversal(root));   // [4, 2, 5, 1, 3]
        System.out.println("后序遍历：" + postorderTraversal(root)); // [4, 5, 2, 3, 1]
    }
}
```

## 复杂度分析
| 遍历方式 | 时间复杂度 | 空间复杂度（递归） | 空间复杂度（迭代） |
| --- | --- | --- | --- |
| 前序遍历 | O(n) | O(h) | O(h) |
| 中序遍历 | O(n) | O(h) | O(h) |
| 后序遍历 | O(n) | O(h) | O(h) 或 O(n) |
| 层序遍历 | O(n) | - | O(w) |


其中：

+ n：节点总数
+ h：树的高度（最坏情况下 h = n）
+ w：树的最大宽度

## 递归 vs 迭代
### 递归的优点
+ 代码简洁，易于理解
+ 自然地体现了树的递归结构
+ 实现简单

### 递归的缺点
+ 可能导致栈溢出（深度过大）
+ 函数调用开销
+ 难以控制遍历过程

### 迭代的优点
+ 避免栈溢出问题
+ 可以控制遍历过程
+ 可以实现懒加载

### 迭代的缺点
+ 代码相对复杂
+ 需要手动管理栈
+ 理解难度较大

## 实际应用
1. **文件系统遍历**

```java
// 前序遍历：先访问目录，再访问子目录
public void traverseDirectory(File dir) {
    System.out.println(dir.getName());  // 访问当前目录
    
    File[] files = dir.listFiles();
    if (files != null) {
        for (File file : files) {
            if (file.isDirectory()) {
                traverseDirectory(file);  // 递归访问子目录
            }
        }
    }
}
```

2. **表达式树求值**

```java
// 后序遍历：先计算子表达式，再计算根表达式
public int evaluateExpressionTree(TreeNode root) {
    if (root == null) return 0;
    
    // 叶子节点是操作数
    if (root.left == null && root.right == null) {
        return root.val;
    }
    
    // 递归计算左右子树
    int leftVal = evaluateExpressionTree(root.left);
    int rightVal = evaluateExpressionTree(root.right);
    
    // 根据操作符计算结果
    switch (root.val) {
        case '+': return leftVal + rightVal;
        case '-': return leftVal - rightVal;
        case '*': return leftVal * rightVal;
        case '/': return leftVal / rightVal;
        default: return 0;
    }
}
```

## 变种问题
1. **Morris遍历**：O(1)空间复杂度的遍历
2. **垂直遍历**：按列遍历二叉树
3. **锯齿形遍历**：层序遍历的变种
4. **边界遍历**：只遍历二叉树的边界节点

## 总结
二叉树遍历是理解树结构的基础，掌握了这些基本遍历方法，就能解决大部分与二叉树相关的问题。在实际应用中，要根据具体需求选择合适的遍历方式和实现方法。



> 更新: 2025-09-19 00:53:54  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/ba69512c10dd95866ffbcf2654ce665a>