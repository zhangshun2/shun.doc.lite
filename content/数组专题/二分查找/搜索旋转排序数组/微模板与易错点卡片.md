---
相关文档导航:
  - [数组专题导航](../../数组专题导航.md)
  - [模板索引：旋转数组二分模板](../../../学习方法/模板索引.md#旋转数组二分模板)
  - [LeetCode原题](https://leetcode.cn/problems/search-in-rotated-sorted-array/)
---

# 微模板与易错点卡片｜33 搜索旋转排序数组

## 微模板（Java）
```java
int search(int[] a, int target) {
    int l = 0, r = a.length - 1;
    while (l <= r) {
        int mid = (l + r) >>> 1;
        if (a[mid] == target) return mid;
        if (a[l] <= a[mid]) { // 左半有序
            if (a[l] <= target && target < a[mid]) r = mid - 1; else l = mid + 1;
        } else {              // 右半有序
            if (a[mid] < target && target <= a[r]) l = mid + 1; else r = mid - 1;
        }
    }
    return -1;
}
```

## 易错点卡片
- 边界比较要含等号，处理单元素与相等情况。
- 本题默认无重复；若存在重复需改用另一套判定避免陷入两侧不确定。
- `>>>1` 避免溢出；也可用 `l + (r - l) / 2`。