---
相关文档导航:
  - [数组专题导航](../../数组专题导航.md)
  - [模板索引：排序加双指针模板](../../../学习方法/模板索引.md#排序加双指针模板)
  - [LeetCode原题](https://leetcode.cn/problems/3sum/)
---

# 微模板与易错点卡片｜15 三数之和

## 微模板（Java）
```java
List<List<Integer>> threeSum(int[] nums) {
    Arrays.sort(nums);
    List<List<Integer>> res = new ArrayList<>();
    int n = nums.length;
    for (int i = 0; i < n; i++) {
        if (i > 0 && nums[i] == nums[i - 1]) continue; // 去重 i
        int l = i + 1, r = n - 1;
        while (l < r) {
            int sum = nums[i] + nums[l] + nums[r];
            if (sum < 0) l++;
            else if (sum > 0) r--;
            else {
                res.add(Arrays.asList(nums[i], nums[l], nums[r]));
                while (l < r && nums[l] == nums[l + 1]) l++; // 去重 l
                while (l < r && nums[r] == nums[r - 1]) r--; // 去重 r
                l++; r--;
            }
        }
    }
    return res;
}
```

## 易错点卡片
- 三重去重：外层 `i`、内层 `l/r` 都需跳过重复。
- 排序后再双指针；避免使用哈希集合导致重复结果或超时。
- 边界：数组长度不足 3 直接返回空列表。