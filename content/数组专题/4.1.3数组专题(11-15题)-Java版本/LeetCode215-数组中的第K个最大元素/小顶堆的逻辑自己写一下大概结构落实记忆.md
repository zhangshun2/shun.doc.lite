# 小顶堆的逻辑自己写一下大概结构落实记忆

自己手搓一遍小顶堆的代码方便记忆

```java


public int findKthLargest(int[] nums, int k) {
    PriorityQueue<Integer> heap = new PriorityQueue<>();
    for(int i = 0;i<nums.length;i++){
        if(heap.size()<k){
            heap.offer(nums[i]);
        }else if (nums[i] > heap.peak()){
            head.poll();
            head.offer(nums[i]);
        }
    }
    return head.peak;
}

```

Priority 这个单词的意思是 优先队列



> 更新: 2025-08-28 10:40:37  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/gyhgezk56ynubqw9>