# 栈算法解题模式总结

# 栈算法解题模式总结
## 栈算法的通用解题思路
### 1. 栈算法识别模式
当遇到以下关键词或场景时，考虑使用栈：

| 关键词/场景 | 栈的作用 | 典型问题 |
| --- | --- | --- |
| **"匹配"** | 保存待匹配的元素 | 括号匹配、标签匹配 |
| **"嵌套"** | 处理嵌套结构 | 嵌套函数调用、嵌套列表 |
| **"最近的"** | 找到最近的某个元素 | 最近更大元素、最近更小元素 |
| **"撤销/回退"** | 保存历史状态 | 浏览器历史、编辑器撤销 |
| **"深度优先"** | 模拟递归调用栈 | DFS遍历、路径搜索 |
| **"逆序处理"** | 利用LIFO特性 | 字符串反转、逆波兰表达式 |
| **"临时存储"** | 保存中间结果 | 表达式求值、状态机 |


### 2. 栈算法的解题步骤
#### 通用解题框架
```java
public class StackSolutionTemplate {
    public ResultType solve(InputType input) {
        // 1. 初始化栈和结果容器
        Deque<ElementType> stack = new ArrayDeque<>();
        ResultType result = initializeResult();
        
        // 2. 遍历输入数据
        for (ElementType element : input) {
            // 3. 栈的核心处理逻辑
            while (!stack.isEmpty() && shouldPop(stack.peek(), element)) {
                ElementType popped = stack.pop();
                processPopped(popped, result);
            }
            
            // 4. 处理当前元素
            processCurrentElement(element, result);
            
            // 5. 决定是否入栈
            if (shouldPush(element)) {
                stack.push(element);
            }
        }
        
        // 6. 处理栈中剩余元素
        while (!stack.isEmpty()) {
            processRemaining(stack.pop(), result);
        }
        
        return result;
    }
    
    // 抽象方法，具体问题具体实现
    private boolean shouldPop(ElementType stackTop, ElementType current) { /* ... */ }
    private void processPopped(ElementType popped, ResultType result) { /* ... */ }
    private void processCurrentElement(ElementType element, ResultType result) { /* ... */ }
    private boolean shouldPush(ElementType element) { /* ... */ }
    private void processRemaining(ElementType remaining, ResultType result) { /* ... */ }
}
```

## 栈算法的经典解题模式
### 模式1：匹配模式
#### 解题思路
1. 遇到开始符号（如左括号）→ 入栈
2. 遇到结束符号（如右括号）→ 出栈匹配
3. 最终栈为空 → 匹配成功

#### 代码模板
```java
public class MatchingPattern {
    public boolean isMatching(String s) {
        Deque<Character> stack = new ArrayDeque<>();
        Map<Character, Character> pairs = createPairMap();
        
        for (char c : s.toCharArray()) {
            if (isOpenSymbol(c)) {
                stack.push(c);
            } else if (isCloseSymbol(c)) {
                if (stack.isEmpty() || stack.pop() != pairs.get(c)) {
                    return false;
                }
            }
        }
        
        return stack.isEmpty();
    }
}
```

#### 应用实例
```java
// 1. 基础括号匹配
public boolean isValidParentheses(String s) {
    Deque<Character> stack = new ArrayDeque<>();
    for (char c : s.toCharArray()) {
        if (c == '(' || c == '[' || c == '{') {
            stack.push(c);
        } else {
            if (stack.isEmpty()) return false;
            char top = stack.pop();
            if ((c == ')' && top != '(') || 
                (c == ']' && top != '[') || 
                (c == '}' && top != '{')) {
                return false;
            }
        }
    }
    return stack.isEmpty();
}

// 2. HTML标签匹配
public boolean isValidHTML(String html) {
    Deque<String> stack = new ArrayDeque<>();
    Pattern tagPattern = Pattern.compile("<(/?)([a-zA-Z]+)>");
    Matcher matcher = tagPattern.matcher(html);
    
    while (matcher.find()) {
        String isClosing = matcher.group(1);
        String tagName = matcher.group(2);
        
        if (isClosing.isEmpty()) {
            // 开始标签
            stack.push(tagName);
        } else {
            // 结束标签
            if (stack.isEmpty() || !stack.pop().equals(tagName)) {
                return false;
            }
        }
    }
    
    return stack.isEmpty();
}
```

### 模式2：单调栈模式
#### 解题思路
1. 维护栈的单调性（递增或递减）
2. 当前元素破坏单调性时，弹出栈顶元素
3. 弹出过程中计算结果

#### 代码模板
```java
public class MonotonicStackPattern {
    public int[] nextGreaterElements(int[] nums) {
        int[] result = new int[nums.length];
        Deque<Integer> stack = new ArrayDeque<>(); // 存储索引
        
        // 从右到左遍历（或从左到右，取决于具体问题）
        for (int i = nums.length - 1; i >= 0; i--) {
            // 维护单调递减栈
            while (!stack.isEmpty() && nums[stack.peek()] <= nums[i]) {
                stack.pop();
            }
            
            result[i] = stack.isEmpty() ? -1 : nums[stack.peek()];
            stack.push(i);
        }
        
        return result;
    }
}
```

#### 应用实例
```java
// 1. 下一个更大元素
public int[] nextGreaterElement(int[] nums1, int[] nums2) {
    Map<Integer, Integer> nextGreater = new HashMap<>();
    Deque<Integer> stack = new ArrayDeque<>();
    
    // 构建nums2中每个元素的下一个更大元素映射
    for (int num : nums2) {
        while (!stack.isEmpty() && stack.peek() < num) {
            nextGreater.put(stack.pop(), num);
        }
        stack.push(num);
    }
    
    // 查询nums1中每个元素的结果
    int[] result = new int[nums1.length];
    for (int i = 0; i < nums1.length; i++) {
        result[i] = nextGreater.getOrDefault(nums1[i], -1);
    }
    
    return result;
}

// 2. 柱状图最大矩形面积
public int largestRectangleArea(int[] heights) {
    Deque<Integer> stack = new ArrayDeque<>();
    int maxArea = 0;
    
    for (int i = 0; i <= heights.length; i++) {
        int currentHeight = (i == heights.length) ? 0 : heights[i];
        
        while (!stack.isEmpty() && heights[stack.peek()] > currentHeight) {
            int height = heights[stack.pop()];
            int width = stack.isEmpty() ? i : i - stack.peek() - 1;
            maxArea = Math.max(maxArea, height * width);
        }
        
        stack.push(i);
    }
    
    return maxArea;
}

// 3. 接雨水问题
public int trap(int[] height) {
    Deque<Integer> stack = new ArrayDeque<>();
    int water = 0;
    
    for (int i = 0; i < height.length; i++) {
        while (!stack.isEmpty() && height[i] > height[stack.peek()]) {
            int bottom = stack.pop();
            if (stack.isEmpty()) break;
            
            int distance = i - stack.peek() - 1;
            int boundedHeight = Math.min(height[i], height[stack.peek()]) - height[bottom];
            water += distance * boundedHeight;
        }
        stack.push(i);
    }
    
    return water;
}
```

### 模式3：表达式求值模式
#### 解题思路
1. 操作数栈 + 操作符栈
2. 处理运算符优先级
3. 遇到右括号时计算括号内表达式

#### 代码模板
```java
public class ExpressionEvaluationPattern {
    public int calculate(String s) {
        Deque<Integer> operands = new ArrayDeque<>();
        Deque<Character> operators = new ArrayDeque<>();
        
        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            
            if (Character.isDigit(c)) {
                int num = parseNumber(s, i);
                operands.push(num);
                i = skipNumber(s, i); // 跳过数字的其余部分
            } else if (c == '(') {
                operators.push(c);
            } else if (c == ')') {
                while (operators.peek() != '(') {
                    evaluate(operands, operators);
                }
                operators.pop(); // 移除 '('
            } else if (isOperator(c)) {
                while (!operators.isEmpty() && hasHigherPrecedence(operators.peek(), c)) {
                    evaluate(operands, operators);
                }
                operators.push(c);
            }
        }
        
        while (!operators.isEmpty()) {
            evaluate(operands, operators);
        }
        
        return operands.pop();
    }
    
    private void evaluate(Deque<Integer> operands, Deque<Character> operators) {
        int b = operands.pop();
        int a = operands.pop();
        char op = operators.pop();
        operands.push(applyOperation(a, b, op));
    }
}
```

### 模式4：DFS遍历模式
#### 解题思路
1. 用栈模拟递归调用栈
2. 保存节点和相关状态
3. 深度优先探索

#### 代码模板
```java
public class DFSPattern {
    public List<Integer> dfsTraversal(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        if (root == null) return result;
        
        Deque<TreeNode> stack = new ArrayDeque<>();
        stack.push(root);
        
        while (!stack.isEmpty()) {
            TreeNode node = stack.pop();
            result.add(node.val);
            
            // 注意：先压右子树，再压左子树
            if (node.right != null) stack.push(node.right);
            if (node.left != null) stack.push(node.left);
        }
        
        return result;
    }
}
```

#### 应用实例
```java
// 1. 二叉树路径总和
public boolean hasPathSum(TreeNode root, int targetSum) {
    if (root == null) return false;
    
    Deque<TreeNode> nodeStack = new ArrayDeque<>();
    Deque<Integer> sumStack = new ArrayDeque<>();
    
    nodeStack.push(root);
    sumStack.push(targetSum - root.val);
    
    while (!nodeStack.isEmpty()) {
        TreeNode node = nodeStack.pop();
        int remainingSum = sumStack.pop();
        
        if (node.left == null && node.right == null && remainingSum == 0) {
            return true;
        }
        
        if (node.left != null) {
            nodeStack.push(node.left);
            sumStack.push(remainingSum - node.left.val);
        }
        
        if (node.right != null) {
            nodeStack.push(node.right);
            sumStack.push(remainingSum - node.right.val);
        }
    }
    
    return false;
}

// 2. 岛屿数量
public int numIslands(char[][] grid) {
    if (grid == null || grid.length == 0) return 0;
    
    int count = 0;
    int rows = grid.length, cols = grid[0].length;
    
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            if (grid[i][j] == '1') {
                count++;
                dfsMarkIsland(grid, i, j);
            }
        }
    }
    
    return count;
}

private void dfsMarkIsland(char[][] grid, int startRow, int startCol) {
    Deque<int[]> stack = new ArrayDeque<>();
    stack.push(new int[]{startRow, startCol});
    
    int[][] directions = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}};
    
    while (!stack.isEmpty()) {
        int[] pos = stack.pop();
        int row = pos[0], col = pos[1];
        
        if (row < 0 || row >= grid.length || col < 0 || col >= grid[0].length || 
            grid[row][col] != '1') {
            continue;
        }
        
        grid[row][col] = '0'; // 标记为已访问
        
        for (int[] dir : directions) {
            stack.push(new int[]{row + dir[0], col + dir[1]});
        }
    }
}
```

### 模式5：状态回溯模式
#### 解题思路
1. 用栈保存状态快照
2. 探索失败时回溯到之前状态
3. 继续尝试其他可能性

#### 代码模板
```java
public class BacktrackingPattern {
    public List<List<Integer>> findAllPaths(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        if (root == null) return result;
        
        Deque<TreeNode> nodeStack = new ArrayDeque<>();
        Deque<List<Integer>> pathStack = new ArrayDeque<>();
        
        nodeStack.push(root);
        pathStack.push(Arrays.asList(root.val));
        
        while (!nodeStack.isEmpty()) {
            TreeNode node = nodeStack.pop();
            List<Integer> path = pathStack.pop();
            
            if (isLeaf(node)) {
                result.add(new ArrayList<>(path));
                continue;
            }
            
            if (node.left != null) {
                nodeStack.push(node.left);
                List<Integer> newPath = new ArrayList<>(path);
                newPath.add(node.left.val);
                pathStack.push(newPath);
            }
            
            if (node.right != null) {
                nodeStack.push(node.right);
                List<Integer> newPath = new ArrayList<>(path);
                newPath.add(node.right.val);
                pathStack.push(newPath);
            }
        }
        
        return result;
    }
}
```

## 栈算法的优化策略
### 1. 空间优化技巧
#### 使用索引代替对象
```java
// 优化前：存储对象
Deque<TreeNode> stack = new ArrayDeque<>();

// 优化后：存储索引
Deque<Integer> stack = new ArrayDeque<>();
TreeNode[] nodes = convertToArray(root);
```

#### 复用数据结构
```java
public class StackOptimization {
    private Deque<Integer> reusableStack = new ArrayDeque<>();
    
    public int[] optimizedSolution(int[] nums) {
        reusableStack.clear(); // 复用栈，避免重复创建
        
        // ... 算法逻辑
        
        return result;
    }
}
```

### 2. 时间优化技巧
#### 预处理优化
```java
// 预处理：构建映射表
Map<Character, Character> bracketMap = Map.of(')', '(', ']', '[', '}', '{');

// 避免在循环中重复计算
public boolean isValid(String s) {
    // 使用预处理的映射表
    for (char c : s.toCharArray()) {
        if (bracketMap.containsKey(c)) {
            // 直接查表，O(1)时间
        }
    }
}
```

#### 早期终止
```java
public boolean hasValidStructure(String s) {
    Deque<Character> stack = new ArrayDeque<>();
    
    for (char c : s.toCharArray()) {
        if (isOpenBracket(c)) {
            stack.push(c);
        } else if (isCloseBracket(c)) {
            if (stack.isEmpty()) {
                return false; // 早期终止
            }
            stack.pop();
        }
    }
    
    return stack.isEmpty();
}
```

## 栈算法的调试技巧
### 1. 可视化调试
```java
public class StackVisualizer {
    public static void visualizeStack(Deque<?> stack, String operation) {
        System.out.println("=== " + operation + " ===");
        System.out.println("Stack (top to bottom): " + 
            stack.stream().collect(Collectors.toList()));
        System.out.println("Size: " + stack.size());
        System.out.println();
    }
    
    public static void traceExecution(String input, Function<String, ?> algorithm) {
        System.out.println("Input: " + input);
        System.out.println("Execution trace:");
        Object result = algorithm.apply(input);
        System.out.println("Result: " + result);
    }
}
```

### 2. 断言检查
```java
public class StackAssertions {
    public static void assertStackInvariant(Deque<Integer> stack, String message) {
        // 检查单调栈的单调性
        if (stack.size() > 1) {
            Integer[] array = stack.toArray(new Integer[0]);
            for (int i = 0; i < array.length - 1; i++) {
                assert array[i] >= array[i + 1] : message + ": Stack not monotonic";
            }
        }
    }
    
    public static void assertBalanced(Deque<?> stack, String message) {
        assert stack.isEmpty() : message + ": Stack should be empty for balanced input";
    }
}
```

## 栈算法的常见陷阱
### 1. 空栈检查
```java
// ❌ 错误：没有检查空栈
public int peek(Deque<Integer> stack) {
    return stack.peek(); // 可能返回null
}

// ✅ 正确：检查空栈
public int safePeek(Deque<Integer> stack) {
    if (stack.isEmpty()) {
        throw new IllegalStateException("Stack is empty");
    }
    return stack.peek();
}
```

### 2. 索引越界
```java
// ❌ 错误：可能索引越界
while (!stack.isEmpty()) {
    int index = stack.pop();
    result[index] = nums[index + 1]; // 可能越界
}

// ✅ 正确：边界检查
while (!stack.isEmpty()) {
    int index = stack.pop();
    if (index + 1 < nums.length) {
        result[index] = nums[index + 1];
    }
}
```

### 3. 状态不一致
```java
// ❌ 错误：忘记更新相关状态
stack.pop();
// 忘记更新其他相关变量

// ✅ 正确：同步更新状态
TreeNode node = stack.pop();
currentPath.remove(currentPath.size() - 1); // 同步更新路径
```

通过掌握这些解题模式和优化技巧，您可以更系统地解决各种栈相关的算法问题。



> 更新: 2025-09-19 00:54:57  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/d0ba80f510ccd72f063a2c59354d338b>