# 4.1.8 链表专题(11题)-Java版本

# 4.1.8 链表专题(11题) - Java版本
> 本文档包含LeetCode热题100中链表专题的最后1道题目，提供基础解法和进阶优化版本
>

---

## 📊 题目概览
| 题号 | 题目 | 难度 | 标签 |
| --- | --- | --- | --- |
| 138 | 复制带随机指针的链表 | 🟡 中等 | 哈希表, 链表 |


---

## 🔗 链表节点定义
```java
class Node {
    int val;
    Node next;
    Node random;

    public Node(int val) {
        this.val = val;
        this.next = null;
        this.random = null;
    }
}
```

---

## 1️⃣ 复制带随机指针的链表 (LeetCode 138)
### 📝 题目描述
给你一个长度为 `n` 的链表，每个节点包含一个额外增加的随机指针 `random`，该指针可以指向链表中的任何节点或空节点。

构造这个链表的深拷贝。深拷贝应该正好由 `n` 个全新节点组成，其中每个新节点的值都设为其对应的原节点的值。新节点的 `next` 指针和 `random` 指针也都应指向复制链表中的新节点，并使原链表和复制链表中的这些指针能够表示相同的链表状态。复制链表中的指针都不应指向原链表中的节点。

例如，如果原链表中有 `X` 和 `Y` 两个节点，其中 `X.random --> Y`。那么在复制链表中对应的两个节点 `x` 和 `y`，其中 `x.random --> y`。

返回复制链表的头节点。

**示例 1：**

```plain
输入：head = [[7,null],[13,0],[11,4],[10,2],[1,0]]
输出：[[7,null],[13,0],[11,4],[10,2],[1,0]]
```

**示例 2：**

```plain
输入：head = [[1,1],[2,1]]
输出：[[1,1],[2,1]]
```

**示例 3：**

```plain
输入：head = [[3,null],[3,0],[3,null]]
输出：[[3,null],[3,0],[3,null]]
```

### 🔧 基础解法：哈希表映射
```java
class Solution {
    public Node copyRandomList(Node head) {
        if (head == null) {
            return null;
        }
        
        // 哈希表存储原节点到新节点的映射
        Map<Node, Node> nodeMap = new HashMap<>();
        
        // 第一次遍历：创建所有新节点
        Node current = head;
        while (current != null) {
            nodeMap.put(current, new Node(current.val));
            current = current.next;
        }
        
        // 第二次遍历：设置next和random指针
        current = head;
        while (current != null) {
            Node newNode = nodeMap.get(current);
            
            // 设置next指针
            if (current.next != null) {
                newNode.next = nodeMap.get(current.next);
            }
            
            // 设置random指针
            if (current.random != null) {
                newNode.random = nodeMap.get(current.random);
            }
            
            current = current.next;
        }
        
        return nodeMap.get(head);
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)，其中n是链表长度，需要遍历两次
+ 空间复杂度：O(n)，哈希表存储n个节点的映射关系

### 🚀 进阶解法：递归 + 哈希表
```java
class Solution {
    private Map<Node, Node> visited = new HashMap<>();
    
    public Node copyRandomList(Node head) {
        if (head == null) {
            return null;
        }
        
        // 如果已经复制过该节点，直接返回
        if (visited.containsKey(head)) {
            return visited.get(head);
        }
        
        // 创建新节点
        Node newNode = new Node(head.val);
        
        // 将映射关系加入哈希表
        visited.put(head, newNode);
        
        // 递归复制next和random指针
        newNode.next = copyRandomList(head.next);
        newNode.random = copyRandomList(head.random);
        
        return newNode;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)，包括哈希表和递归调用栈

### 🚀 最优解法：原地复制（O(1)空间）
```java
class Solution {
    public Node copyRandomList(Node head) {
        if (head == null) {
            return null;
        }
        
        // 第一步：在每个原节点后面插入复制节点
        Node current = head;
        while (current != null) {
            Node newNode = new Node(current.val);
            newNode.next = current.next;
            current.next = newNode;
            current = newNode.next;
        }
        
        // 第二步：设置复制节点的random指针
        current = head;
        while (current != null) {
            if (current.random != null) {
                current.next.random = current.random.next;
            }
            current = current.next.next;
        }
        
        // 第三步：分离原链表和复制链表
        Node dummy = new Node(0);
        Node copyPrev = dummy;
        current = head;
        
        while (current != null) {
            Node copyNode = current.next;
            
            // 恢复原链表
            current.next = copyNode.next;
            
            // 构建复制链表
            copyPrev.next = copyNode;
            copyPrev = copyNode;
            
            current = current.next;
        }
        
        return dummy.next;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(1)，不使用额外的哈希表

**解题思路：**

**方法一：哈希表映射**

1. 第一次遍历：为每个原节点创建对应的新节点，建立映射关系
2. 第二次遍历：根据映射关系设置新节点的next和random指针

**方法二：递归**

1. 使用递归的方式深度优先遍历链表
2. 利用哈希表避免重复创建节点
3. 递归处理next和random指针

**方法三：原地复制（最优）**

1. 在每个原节点后插入对应的复制节点
2. 利用原节点和复制节点的相邻关系设置random指针
3. 分离原链表和复制链表

### 💡 详细步骤解析（原地复制法）
**步骤1：插入复制节点**

```plain
原链表：A -> B -> C -> null
插入后：A -> A' -> B -> B' -> C -> C' -> null
```

**步骤2：设置random指针**

```plain
如果A.random = C，则A'.random = C'
由于A'紧跟在A后面，C'紧跟在C后面
所以A'.random = A.random.next
```

**步骤3：分离链表**

```plain
分离前：A -> A' -> B -> B' -> C -> C' -> null
分离后：
原链表：A -> B -> C -> null
复制链表：A' -> B' -> C' -> null
```

### 🔍 代码优化版本
```java
class Solution {
    public Node copyRandomList(Node head) {
        if (head == null) return null;
        
        // 步骤1：创建复制节点并插入
        for (Node curr = head; curr != null; curr = curr.next.next) {
            Node copy = new Node(curr.val);
            copy.next = curr.next;
            curr.next = copy;
        }
        
        // 步骤2：设置random指针
        for (Node curr = head; curr != null; curr = curr.next.next) {
            if (curr.random != null) {
                curr.next.random = curr.random.next;
            }
        }
        
        // 步骤3：分离链表
        Node copyHead = head.next;
        for (Node curr = head; curr != null; curr = curr.next) {
            Node copy = curr.next;
            curr.next = copy.next;
            if (copy.next != null) {
                copy.next = copy.next.next;
            }
        }
        
        return copyHead;
    }
}
```

---

## 📚 总结
### 🎯 核心知识点
1. **深拷贝概念**：创建完全独立的副本，不共享任何引用
2. **哈希表映射**：建立原节点到新节点的对应关系
3. **递归遍历**：深度优先的方式处理复杂结构
4. **原地算法**：巧妙利用空间，实现O(1)空间复杂度
5. **指针操作**：复杂的指针重连和链表分离

### 💡 解题模式
+ **映射思想**：用哈希表建立新旧节点对应关系
+ **分步处理**：先创建节点，再设置指针关系
+ **递归优化**：利用递归简化代码逻辑
+ **空间优化**：原地操作减少额外空间使用
+ **链表操作**：插入、分离、重连等基本操作

### 🔄 刷题建议
1. **理解深拷贝**：区分浅拷贝和深拷贝的概念
2. **掌握哈希表**：熟练使用哈希表建立映射关系
3. **练习递归**：理解递归在图遍历中的应用
4. **空间优化思维**：学会用巧妙方法减少空间复杂度
5. **复杂指针操作**：处理多重指针关系的能力

### 🎯 相关题目
+ **LeetCode 133**：克隆图（类似的深拷贝问题）
+ **LeetCode 1485**：克隆含随机指针的二叉树
+ **LeetCode 430**：扁平化多级双向链表

---

## 🎉 链表专题完结
恭喜你完成了LeetCode热题100中所有11道链表题目的学习！

### 📈 学习成果
+ ✅ 掌握了链表的基本操作：插入、删除、遍历
+ ✅ 学会了双指针技巧：快慢指针、前后指针
+ ✅ 理解了递归在链表中的应用
+ ✅ 熟悉了链表翻转、合并、分割等高级操作
+ ✅ 掌握了复杂数据结构的深拷贝技巧

### 🚀 下一步学习
建议继续学习：

+ **二叉树专题**：树的遍历和递归算法
+ **栈专题**：栈的应用和单调栈
+ **动态规划专题**：状态转移和优化策略

### 💪 链表核心技能总结
1. **基础操作**：增删改查、遍历
2. **双指针**：快慢指针、滑动窗口
3. **递归思维**：分治、回溯
4. **空间优化**：原地算法、常数空间
5. **复杂结构**：多重指针、图结构

---

**上一篇：** [4.1.7 链表专题(6-10题) - Java版本](./4.1.7%20链表专题(6-10题)-Java版本.md)  
**下一篇：** [4.1.9 二叉树专题(1-5题) - Java版本](./4.1.9%20二叉树专题(1-5题)-Java版本.md)



> 更新: 2025-08-25 19:00:54  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/7a5dcfc4ee589b1d4bbeecf0b06e7aeb>