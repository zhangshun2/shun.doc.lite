# 5.2 动态规划进阶-打家劫舍例题详解-Java版本

# 5.2 动态规划进阶 - 打家劫舍例题详解 (Java版本)
> 应用爬楼梯的思路，解决更复杂的DP问题
>

## 🚀 题目：198-打家劫舍 (应用相同思路)
### 🔍 题目描述
你是一个专业的小偷，计划偷窃沿街的房屋。每间房内都藏有一定的现金，影响你偷窃的唯一制约因素就是相邻的房屋装有相互连通的防盗系统，如果两间相邻的房屋在同一晚上被小偷闯入，系统会自动报警。

给定一个代表每个房屋存放金额的非负整数数组，计算你不触动警报装置的情况下，一夜之间能够偷窃到的最高金额。

**示例：**

+ 输入：nums = [2,7,9,3,1]
+ 输出：12
+ 解释：偷窃 1 号房屋 (金额 = 2), 偷窃 3 号房屋 (金额 = 9)，接着偷窃 5 号房屋 (金额 = 1)。偷窃到的最高金额 = 2 + 9 + 1 = 12 。

### 🤔 用爬楼梯的思路来分析
**关键问题：到第i个房子时，能偷到的最大金额是多少？**

**思考过程：**

1. **状态定义：** dp[i] = 到第i个房子时能偷到的最大金额
2. **决策分析：** 对于第i个房子，我有两个选择：
    - **偷第i个房子：** 那么第i-1个房子就不能偷，最大金额 = dp[i-2] + nums[i]
    - **不偷第i个房子：** 最大金额 = dp[i-1]
3. **递推关系：** dp[i] = max(dp[i-1], dp[i-2] + nums[i])

### 💡 详细推导过程
以 nums = [2,7,9,3,1] 为例：

**手工计算：**

+ dp[0] = 2 (只有第0个房子，必须偷)
+ dp[1] = max(dp[0], nums[1]) = max(2, 7) = 7 (偷第1个房子更划算)
+ dp[2] = max(dp[1], dp[0] + nums[2]) = max(7, 2+9) = 11 (偷第0和第2个房子)
+ dp[3] = max(dp[2], dp[1] + nums[3]) = max(11, 7+3) = 11 (不偷第3个房子)
+ dp[4] = max(dp[3], dp[2] + nums[4]) = max(11, 11+1) = 12 (偷第4个房子)

**验证：**  
最优方案是偷第0、2、4个房子：2 + 9 + 1 = 12 ✓

### 🔧 代码实现
#### 完整版本
```java
public class Solution {
    public int rob(int[] nums) {
        if (nums == null || nums.length == 0) {
            return 0;
        }
        if (nums.length == 1) {
            return nums[0];
        }
        
        int n = nums.length;
        int[] dp = new int[n];
        
        // 边界条件
        dp[0] = nums[0];
        dp[1] = Math.max(nums[0], nums[1]);
        
        // 填充dp数组
        for (int i = 2; i < n; i++) {
            dp[i] = Math.max(dp[i - 1], dp[i - 2] + nums[i]);
        }
        
        return dp[n - 1];
    }
}
```

#### 空间优化版本
```java
public class Solution {
    public int rob(int[] nums) {
        if (nums == null || nums.length == 0) {
            return 0;
        }
        if (nums.length == 1) {
            return nums[0];
        }
        
        // 只需要记录前两个状态
        int prev2 = nums[0];  // dp[i-2]
        int prev1 = Math.max(nums[0], nums[1]);  // dp[i-1]
        
        for (int i = 2; i < nums.length; i++) {
            int current = Math.max(prev1, prev2 + nums[i]);
            prev2 = prev1;
            prev1 = current;
        }
        
        return prev1;
    }
}
```

---

## 🎯 从爬楼梯到打家劫舍的思路迁移
### 🔄 相同点
1. **都是线性DP**：状态按顺序推进
2. **都有选择**：爬楼梯选择爬1步或2步，打家劫舍选择偷或不偷
3. **都有约束**：爬楼梯每次最多2步，打家劫舍不能偷相邻房子
4. **都有递推关系**：当前状态依赖于前面的状态

### 🔄 不同点
1. **状态含义**：
    - 爬楼梯：到达第n阶的方法数
    - 打家劫舍：到第i个房子的最大金额
2. **递推关系**：
    - 爬楼梯：f(n) = f(n-1) + f(n-2) (加法)
    - 打家劫舍：dp[i] = max(dp[i-1], dp[i-2] + nums[i]) (取最大值)

### 🎯 思路迁移的关键
1. **明确状态定义**：dp[i]代表什么？
2. **分析决策选择**：在第i步有哪些选择？
3. **找出约束条件**：什么情况下不能选择？
4. **建立递推关系**：当前状态如何从之前状态得出？

---

## 💡 关键思路总结
### 🎯 DP四步解题法
1. **状态定义**：dp[i] = 到第i个房子时能偷到的最大金额
2. **状态转移**：dp[i] = max(dp[i-1], dp[i-2] + nums[i])
3. **边界条件**：dp[0] = nums[0], dp[1] = max(nums[0], nums[1])
4. **计算顺序**：从小到大

### 🔧 Java编程要点
1. **null检查**：处理空数组和null数组
2. **边界处理**：单个元素的特殊情况
3. **Math.max()使用**：取最大值的标准写法
4. **空间优化**：只保存必要的前置状态

---

_学习时间：2024年12月_

> 💪 打家劫舍展示了DP思路的迁移能力，掌握了基本模式就能举一反三！
>



> 更新: 2025-08-25 19:01:07  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/e0e1cd3167af01af165f0e15e93bca7b>