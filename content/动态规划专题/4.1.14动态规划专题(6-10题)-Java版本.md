# 4.1.14 动态规划专题(6-10题)-Java版本

# 4.1.14 动态规划专题(6-10题) - Java版本
> 本文档包含LeetCode热题100中动态规划专题的第6-10道题目，提供基础解法和进阶优化版本
>

---

## 📊 题目概览
| 题号 | 题目 | 难度 | 标签 |
| --- | --- | --- | --- |
| 139 | 单词拆分 | 🟡 中等 | 字典树, 记忆化搜索, 数组, 哈希表, 字符串, 动态规划 |
| 300 | 最长递增子序列 | 🟡 中等 | 数组, 二分查找, 动态规划 |
| 416 | 分割等和子集 | 🟡 中等 | 数组, 动态规划 |
| 32 | 最长有效括号 | 🔴 困难 | 栈, 字符串, 动态规划 |
| 72 | 编辑距离 | 🔴 困难 | 字符串, 动态规划 |


---

## 🎯 高级动态规划模式
### 🔑 常见DP类型
1. **字符串DP**：单词拆分、编辑距离等
2. **序列DP**：最长递增子序列等
3. **背包DP**：分割等和子集等
4. **区间DP**：最长有效括号等
5. **状态机DP**：股票买卖等

### 📝 优化技巧
1. **空间压缩**：一维数组代替二维数组
2. **状态转移优化**：减少不必要的计算
3. **二分查找优化**：LIS问题的O(nlogn)解法
4. **滚动数组**：只保留必要的历史状态

---

## 6️⃣ 单词拆分 (LeetCode 139)
### 📝 题目描述
给你一个字符串 `s` 和一个字符串列表 `wordDict` 作为字典。请你判断是否可以利用字典中出现的单词拼接出 `s`。

**注意：**不要求字典中出现的单词全部都使用，并且字典中的单词可以重复使用。

**示例 1：**

```plain
输入: s = "leetcode", wordDict = ["leet","code"]
输出: true
解释: 返回 true 因为 "leetcode" 可以由 "leet" 和 "code" 拼接成。
```

**示例 2：**

```plain
输入: s = "applepenapple", wordDict = ["apple","pen"]
输出: true
解释: 返回 true 因为 "applepenapple" 可以由 "apple" "pen" "apple" 拼接成。
     注意，你可以重复使用字典中的单词。
```

**示例 3：**

```plain
输入: s = "catsandog", wordDict = ["cats","dog","sand","and","cat"]
输出: false
```

### 🔧 基础解法：动态规划
```java
class Solution {
    public boolean wordBreak(String s, List<String> wordDict) {
        Set<String> wordSet = new HashSet<>(wordDict);
        int n = s.length();
        
        // dp[i] 表示字符串s的前i个字符能否被拆分
        boolean[] dp = new boolean[n + 1];
        dp[0] = true; // 空字符串可以被拆分
        
        for (int i = 1; i <= n; i++) {
            for (int j = 0; j < i; j++) {
                // 如果s[0...j-1]可以被拆分，且s[j...i-1]在字典中
                if (dp[j] && wordSet.contains(s.substring(j, i))) {
                    dp[i] = true;
                    break;
                }
            }
        }
        
        return dp[n];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n³)，其中n是字符串长度
+ 空间复杂度：O(n + m)，m是字典大小

### 🚀 进阶解法：优化的动态规划
```java
class Solution {
    public boolean wordBreak(String s, List<String> wordDict) {
        Set<String> wordSet = new HashSet<>(wordDict);
        int n = s.length();
        boolean[] dp = new boolean[n + 1];
        dp[0] = true;
        
        // 预计算最大单词长度，减少不必要的检查
        int maxLen = 0;
        for (String word : wordDict) {
            maxLen = Math.max(maxLen, word.length());
        }
        
        for (int i = 1; i <= n; i++) {
            for (int j = Math.max(0, i - maxLen); j < i; j++) {
                if (dp[j] && wordSet.contains(s.substring(j, i))) {
                    dp[i] = true;
                    break;
                }
            }
        }
        
        return dp[n];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n * m * k)，k是平均单词长度
+ 空间复杂度：O(n + m)

### 🚀 最优解法：记忆化搜索
```java
class Solution {
    private Set<String> wordSet;
    private Boolean[] memo;
    
    public boolean wordBreak(String s, List<String> wordDict) {
        wordSet = new HashSet<>(wordDict);
        memo = new Boolean[s.length()];
        return dfs(s, 0);
    }
    
    private boolean dfs(String s, int start) {
        if (start == s.length()) {
            return true;
        }
        
        if (memo[start] != null) {
            return memo[start];
        }
        
        for (int end = start + 1; end <= s.length(); end++) {
            String word = s.substring(start, end);
            if (wordSet.contains(word) && dfs(s, end)) {
                memo[start] = true;
                return true;
            }
        }
        
        memo[start] = false;
        return false;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n²)
+ 空间复杂度：O(n)

---

## 7️⃣ 最长递增子序列 (LeetCode 300)
### 📝 题目描述
给你一个整数数组 `nums`，找到其中最长严格递增子序列的长度。

**子序列** 是由数组派生而来的序列，删除（或不删除）数组中的元素而不改变其余元素的顺序。例如，`[3,6,2,7]` 是数组 `[0,3,1,6,2,2,7]` 的子序列。

**示例 1：**

```plain
输入：nums = [10,9,2,5,3,7,101,18]
输出：4
解释：最长递增子序列是 [2,3,7,18]，因此长度为 4 。
```

**示例 2：**

```plain
输入：nums = [0,1,0,3,2,3]
输出：4
```

**示例 3：**

```plain
输入：nums = [7,7,7,7,7,7,7]
输出：1
```

### 🔧 基础解法：动态规划
```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        if (nums.length == 0) {
            return 0;
        }
        
        int n = nums.length;
        // dp[i] 表示以nums[i]结尾的最长递增子序列长度
        int[] dp = new int[n];
        Arrays.fill(dp, 1); // 每个元素自身构成长度为1的子序列
        
        int maxLen = 1;
        
        for (int i = 1; i < n; i++) {
            for (int j = 0; j < i; j++) {
                if (nums[j] < nums[i]) {
                    dp[i] = Math.max(dp[i], dp[j] + 1);
                }
            }
            maxLen = Math.max(maxLen, dp[i]);
        }
        
        return maxLen;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n²)
+ 空间复杂度：O(n)

### 🚀 进阶解法：二分查找优化
```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        if (nums.length == 0) {
            return 0;
        }
        
        // tails[i] 表示长度为i+1的递增子序列的最小尾部元素
        List<Integer> tails = new ArrayList<>();
        
        for (int num : nums) {
            // 二分查找第一个大于等于num的位置
            int left = 0, right = tails.size();
            while (left < right) {
                int mid = left + (right - left) / 2;
                if (tails.get(mid) < num) {
                    left = mid + 1;
                } else {
                    right = mid;
                }
            }
            
            // 如果num比所有元素都大，添加到末尾
            if (left == tails.size()) {
                tails.add(num);
            } else {
                // 否则替换第一个大于等于num的元素
                tails.set(left, num);
            }
        }
        
        return tails.size();
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n log n)
+ 空间复杂度：O(n)

### 🚀 最优解法：使用Arrays.binarySearch
```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        int[] tails = new int[nums.length];
        int size = 0;
        
        for (int num : nums) {
            // 使用二分查找找到插入位置
            int index = Arrays.binarySearch(tails, 0, size, num);
            
            // 如果没找到，binarySearch返回 -(插入点) - 1
            if (index < 0) {
                index = -(index + 1);
            }
            
            tails[index] = num;
            
            // 如果插入位置是末尾，增加size
            if (index == size) {
                size++;
            }
        }
        
        return size;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n log n)
+ 空间复杂度：O(n)

---

## 8️⃣ 分割等和子集 (LeetCode 416)
### 📝 题目描述
给你一个 **只包含正整数** 的 **非空** 数组 `nums`。请你判断是否可以将这个数组分割成两个子集，使得两个子集的元素和相等。

**示例 1：**

```plain
输入：nums = [1,5,11,5]
输出：true
解释：数组可以分割成 [1, 5, 5] 和 [11] 。
```

**示例 2：**

```plain
输入：nums = [1,2,3,5]
输出：false
解释：数组不能分割成两个元素和相等的子集。
```

### 🔧 基础解法：二维动态规划
```java
class Solution {
    public boolean canPartition(int[] nums) {
        int sum = 0;
        for (int num : nums) {
            sum += num;
        }
        
        // 如果总和是奇数，无法分割成两个相等的子集
        if (sum % 2 != 0) {
            return false;
        }
        
        int target = sum / 2;
        int n = nums.length;
        
        // dp[i][j] 表示前i个数字能否组成和为j
        boolean[][] dp = new boolean[n + 1][target + 1];
        
        // 初始化：和为0总是可以达到（选择空集）
        for (int i = 0; i <= n; i++) {
            dp[i][0] = true;
        }
        
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= target; j++) {
                // 不选择第i个数字
                dp[i][j] = dp[i - 1][j];
                
                // 选择第i个数字（如果可能）
                if (j >= nums[i - 1]) {
                    dp[i][j] = dp[i][j] || dp[i - 1][j - nums[i - 1]];
                }
            }
        }
        
        return dp[n][target];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n * sum)
+ 空间复杂度：O(n * sum)

### 🚀 进阶解法：一维动态规划
```java
class Solution {
    public boolean canPartition(int[] nums) {
        int sum = 0;
        for (int num : nums) {
            sum += num;
        }
        
        if (sum % 2 != 0) {
            return false;
        }
        
        int target = sum / 2;
        
        // dp[j] 表示能否组成和为j
        boolean[] dp = new boolean[target + 1];
        dp[0] = true;
        
        for (int num : nums) {
            // 从后往前遍历，避免重复使用同一个数字
            for (int j = target; j >= num; j--) {
                dp[j] = dp[j] || dp[j - num];
            }
        }
        
        return dp[target];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n * sum)
+ 空间复杂度：O(sum)

### 🚀 最优解法：位运算优化
```java
class Solution {
    public boolean canPartition(int[] nums) {
        int sum = 0;
        for (int num : nums) {
            sum += num;
        }
        
        if (sum % 2 != 0) {
            return false;
        }
        
        int target = sum / 2;
        
        // 使用BitSet优化空间和时间
        java.util.BitSet dp = new java.util.BitSet(target + 1);
        dp.set(0); // dp[0] = true
        
        for (int num : nums) {
            // 左移num位相当于对每个可达到的和加上num
            dp.or(dp.get(0, target + 1 - num).stream()
                .mapToObj(i -> i + num)
                .collect(java.util.BitSet::new, 
                        java.util.BitSet::set, 
                        java.util.BitSet::or));
        }
        
        return dp.get(target);
    }
}
```

**更简洁的位运算版本：**

```java
class Solution {
    public boolean canPartition(int[] nums) {
        int sum = Arrays.stream(nums).sum();
        if (sum % 2 != 0) return false;
        
        int target = sum / 2;
        boolean[] dp = new boolean[target + 1];
        dp[0] = true;
        
        for (int num : nums) {
            for (int j = target; j >= num; j--) {
                dp[j] |= dp[j - num];
            }
            if (dp[target]) return true; // 提前退出优化
        }
        
        return dp[target];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n * sum)，但实际运行更快
+ 空间复杂度：O(sum)

---

## 9️⃣ 最长有效括号 (LeetCode 32)
### 📝 题目描述
给你一个只包含 `'('` 和 `')'` 的字符串，找出最长有效（格式正确且连续）括号子串的长度。

**示例 1：**

```plain
输入：s = "(()"
输出：2
解释：最长有效括号子串是 "()"
```

**示例 2：**

```plain
输入：s = ")()())"
输出：4
解释：最长有效括号子串是 "()()"
```

**示例 3：**

```plain
输入：s = ""
输出：0
```

### 🔧 基础解法：动态规划
```java
class Solution {
    public int longestValidParentheses(String s) {
        int n = s.length();
        if (n <= 1) {
            return 0;
        }
        
        // dp[i] 表示以s[i]结尾的最长有效括号长度
        int[] dp = new int[n];
        int maxLen = 0;
        
        for (int i = 1; i < n; i++) {
            if (s.charAt(i) == ')') {
                if (s.charAt(i - 1) == '(') {
                    // 情况1: ...() 
                    dp[i] = (i >= 2 ? dp[i - 2] : 0) + 2;
                } else if (dp[i - 1] > 0) {
                    // 情况2: ...)) 
                    // 找到与当前')'匹配的'('的位置
                    int matchIndex = i - dp[i - 1] - 1;
                    if (matchIndex >= 0 && s.charAt(matchIndex) == '(') {
                        dp[i] = dp[i - 1] + 2 + (matchIndex > 0 ? dp[matchIndex - 1] : 0);
                    }
                }
            }
            maxLen = Math.max(maxLen, dp[i]);
        }
        
        return maxLen;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 进阶解法：栈
```java
class Solution {
    public int longestValidParentheses(String s) {
        Stack<Integer> stack = new Stack<>();
        stack.push(-1); // 初始化栈底
        int maxLen = 0;
        
        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == '(') {
                stack.push(i);
            } else {
                stack.pop();
                if (stack.isEmpty()) {
                    // 栈为空，说明当前')'无法匹配
                    stack.push(i);
                } else {
                    // 计算当前有效括号长度
                    maxLen = Math.max(maxLen, i - stack.peek());
                }
            }
        }
        
        return maxLen;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 最优解法：双向遍历
```java
class Solution {
    public int longestValidParentheses(String s) {
        int left = 0, right = 0, maxLen = 0;
        
        // 从左到右遍历
        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == '(') {
                left++;
            } else {
                right++;
            }
            
            if (left == right) {
                maxLen = Math.max(maxLen, 2 * right);
            } else if (right > left) {
                left = right = 0;
            }
        }
        
        left = right = 0;
        
        // 从右到左遍历
        for (int i = s.length() - 1; i >= 0; i--) {
            if (s.charAt(i) == '(') {
                left++;
            } else {
                right++;
            }
            
            if (left == right) {
                maxLen = Math.max(maxLen, 2 * left);
            } else if (left > right) {
                left = right = 0;
            }
        }
        
        return maxLen;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(1)

---

## 🔟 编辑距离 (LeetCode 72)
### 📝 题目描述
给你两个单词 `word1` 和 `word2`， _请返回将 _`word1`_ 转换成 _`word2`_ 所使用的最少操作数_ 。

你可以对一个单词进行如下三种操作：

+ 插入一个字符
+ 删除一个字符
+ 替换一个字符

**示例 1：**

```plain
输入：word1 = "horse", word2 = "ros"
输出：3
解释：
horse -> rorse (将 'h' 替换为 'r')
rorse -> rose (删除 'r')
rose -> ros (删除 'e')
```

**示例 2：**

```plain
输入：word1 = "intention", word2 = "execution"
输出：5
解释：
intention -> inention (删除 't')
inention -> enention (将 'i' 替换为 'e')
enention -> exention (将 'n' 替换为 'x')
exention -> exection (将 'n' 替换为 'c')
exection -> execution (插入 'u')
```

### 🔧 基础解法：二维动态规划
```java
class Solution {
    public int minDistance(String word1, String word2) {
        int m = word1.length();
        int n = word2.length();
        
        // dp[i][j] 表示word1前i个字符转换为word2前j个字符的最少操作数
        int[][] dp = new int[m + 1][n + 1];
        
        // 初始化边界条件
        for (int i = 0; i <= m; i++) {
            dp[i][0] = i; // word1前i个字符转换为空字符串需要i次删除
        }
        for (int j = 0; j <= n; j++) {
            dp[0][j] = j; // 空字符串转换为word2前j个字符需要j次插入
        }
        
        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                if (word1.charAt(i - 1) == word2.charAt(j - 1)) {
                    // 字符相同，不需要操作
                    dp[i][j] = dp[i - 1][j - 1];
                } else {
                    // 字符不同，选择代价最小的操作
                    dp[i][j] = Math.min(
                        Math.min(
                            dp[i - 1][j] + 1,     // 删除word1[i-1]
                            dp[i][j - 1] + 1      // 插入word2[j-1]
                        ),
                        dp[i - 1][j - 1] + 1      // 替换word1[i-1]为word2[j-1]
                    );
                }
            }
        }
        
        return dp[m][n];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(m * n)
+ 空间复杂度：O(m * n)

### 🚀 进阶解法：空间优化
```java
class Solution {
    public int minDistance(String word1, String word2) {
        int m = word1.length();
        int n = word2.length();
        
        // 只需要保存两行
        int[] prev = new int[n + 1];
        int[] curr = new int[n + 1];
        
        // 初始化第一行
        for (int j = 0; j <= n; j++) {
            prev[j] = j;
        }
        
        for (int i = 1; i <= m; i++) {
            curr[0] = i; // 第一列
            
            for (int j = 1; j <= n; j++) {
                if (word1.charAt(i - 1) == word2.charAt(j - 1)) {
                    curr[j] = prev[j - 1];
                } else {
                    curr[j] = Math.min(
                        Math.min(prev[j] + 1, curr[j - 1] + 1),
                        prev[j - 1] + 1
                    );
                }
            }
            
            // 交换数组
            int[] temp = prev;
            prev = curr;
            curr = temp;
        }
        
        return prev[n];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(m * n)
+ 空间复杂度：O(n)

### 🚀 最优解法：一维数组优化
```java
class Solution {
    public int minDistance(String word1, String word2) {
        int m = word1.length();
        int n = word2.length();
        
        // 确保word2是较短的字符串，减少空间复杂度
        if (m < n) {
            return minDistance(word2, word1);
        }
        
        int[] dp = new int[n + 1];
        
        // 初始化
        for (int j = 0; j <= n; j++) {
            dp[j] = j;
        }
        
        for (int i = 1; i <= m; i++) {
            int prev = dp[0]; // 保存dp[i-1][j-1]
            dp[0] = i;
            
            for (int j = 1; j <= n; j++) {
                int temp = dp[j]; // 保存当前dp[j]，下次循环作为prev
                
                if (word1.charAt(i - 1) == word2.charAt(j - 1)) {
                    dp[j] = prev;
                } else {
                    dp[j] = Math.min(
                        Math.min(dp[j] + 1, dp[j - 1] + 1),
                        prev + 1
                    );
                }
                
                prev = temp;
            }
        }
        
        return dp[n];
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(m * n)
+ 空间复杂度：O(min(m, n))

---

## 📚 总结
### 🎯 核心知识点
1. **字符串DP**：单词拆分、编辑距离等字符串处理问题
2. **序列DP**：最长递增子序列等序列优化问题
3. **背包DP**：分割等和子集等背包变种问题
4. **区间DP**：最长有效括号等区间处理问题
5. **空间优化**：滚动数组、一维数组等空间压缩技巧

### 💡 解题模式
+ **字符串匹配DP**：编辑距离、单词拆分等
+ **序列优化DP**：LIS的二分查找优化
+ **0-1背包DP**：分割等和子集等
+ **多解法对比**：DP、栈、双指针等不同方法
+ **状态压缩**：二维转一维的空间优化

### 🔄 刷题建议
1. **掌握经典模型**：背包、LIS、编辑距离等
2. **学会空间优化**：滚动数组等技巧
3. **理解状态转移**：准确分析状态关系
4. **练习多种解法**：DP、贪心、二分等
5. **注意边界条件**：空字符串、单元素等特殊情况

### 🎯 相关题目
**字符串DP：**

+ **LeetCode 140**：单词拆分 II
+ **LeetCode 97**：交错字符串

**序列DP：**

+ **LeetCode 673**：最长递增子序列的个数
+ **LeetCode 354**：俄罗斯套娃信封问题

**背包DP：**

+ **LeetCode 494**：目标和
+ **LeetCode 1049**：最后一块石头的重量 II

---

**上一篇：** [4.1.13 动态规划专题(1-5题) - Java版本](./4.1.13%20动态规划专题(1-5题)-Java版本.md)  
**下一篇：** [4.1.15 动态规划专题(11-15题) - Java版本](./4.1.15%20动态规划专题(11-15题)-Java版本.md)



> 更新: 2025-08-25 19:00:27  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/e60dd14434d6652cd00f3e560eee8b90>