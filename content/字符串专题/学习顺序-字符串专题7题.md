# 字符串专题学习顺序（7题路径）

## 相关文档导航
- [滑动窗口导航](./滑动窗口/滑动窗口导航.md)
- [双指针-中心扩展导航](./双指针-中心扩展/双指针-中心扩展导航.md)
- [动态规划-字符串导航](./动态规划-字符串/动态规划-字符串导航.md)
- [哈希与排序导航](./哈希与排序/哈希与排序导航.md)
- [字母异位词分组-问题](./哈希与排序/字母异位词分组/问题1.0.0-字母异位词分组.md)
- [字母异位词分组-题解](./哈希与排序/字母异位词分组/题解2.0.0-字母异位词分组.md)
- [字符索引映射：ch - 'a' 是什么](./哈希与排序/字母异位词分组/问题1.0.0-字符索引映射-ch减a是什么.md)
- [模板索引](../../学习方法/模板索引.md)

## 学习顺序一览（由易到难，按套路递进）
1) 49 字母异位词分组（哈希分组｜签名构造，快速上手）
2) 3 无重复字符的最长子串（滑动窗口入门｜窗口内去重）
3) 647 回文子串（中心扩展入门｜计数所有回文）
4) 5 最长回文子串（中心扩展进阶｜维护最长区间）
5) 76 最小覆盖子串（滑动窗口进阶｜need/window 计数与收缩）
6) 72 编辑距离（二维DP入门｜三操作转移）
7) 10 正则表达式匹配（字符串DP综合｜`*` 的零/多次处理）

## 套路速通与骨架

### 哈希分组（49）
- 核心：为每个字符串构造“签名（signature）”，同签名聚一组。
- 两法：
  - 排序签名：对字符排序后作为键（简单，单串 `O(L log L)`）。
  - 计数签名：`int[26]` 统计频次并序列化为键（高效，单串 `O(L)`）。
- 先修：`ch - 'a'` 索引映射，见概念文档。

### 滑动窗口（3、76）
- 骨架：
  - `for (r) { add(s[r]); while (条件满足) { update ans; remove(s[l]); l++; } }`
- 3 题关键：窗口内不能重复，常用 `Set/Map` 维护计数；条件是“出现次数 <= 1”。
- 76 题关键：
  - 维护 `need`（目标字符计数）与 `window`（当前窗口计数）。
  - `valid` 表示窗口是否满足所有 `need`。
  - 满足后尽量收缩以取最短。

### 中心扩展（647、5）
- 骨架：
  - `expand(l, r) { while (l>=0 && r<n && s[l]==s[r]) { l--; r++; } }`
- 647：枚举所有中心（奇/偶），扩展时每次找到回文就计数。
- 5：同样扩展，但维护 `[bestL, bestR]` 最长区间。

### 字符串DP（72、10）
- 72 编辑距离：
  - `dp[i][j]`：`s[0..i)` 到 `p[0..j)` 的最少操作数。
  - 转移：`min( dp[i-1][j]+1, dp[i][j-1]+1, dp[i-1][j-1]+cost )`，`cost=0/1`（相等/替换）。
- 10 正则匹配：
  - `dp[i][j]`：`s[0..i)` 是否匹配 `p[0..j)`。
  - 非 `*`：`dp[i][j] = match(s[i-1], p[j-1]) && dp[i-1][j-1]`。
  - `*`：`dp[i][j] = dp[i][j-2] || ( match(s[i-1], p[j-2]) && dp[i-1][j] )`。
  - 初始化空串：`dp[0][0]=true`；`dp[0][j]` 仅当前缀形如 `a*b*c*`。

## 先修技能与推荐阅读
- 字符索引映射：`ch - 'a'`（数组计数更高效）
  - 文档：./哈希与排序/字母异位词分组/问题1.0.0-字符索引映射-ch减a是什么.md
- 模板索引：
  - 滑动窗口模板：`../../学习方法/模板索引.md#滑动窗口模板`
  - 中心扩展模板：`../../学习方法/模板索引.md#中心扩展模板`
  - 字符串DP状态机模板：`../../学习方法/模板索引.md#字符串dp状态机模板`
  - 编辑距离DP模板：`../../学习方法/模板索引.md#编辑距离dp模板`

## 常见坑与校验
- 滑动窗口：何时收缩、何时更新答案，条件写反最常见。
- 中心扩展：奇偶中心都要覆盖；越界检查要完整。
- 哈希键序列化：频次向量必须加分隔符（如 `#`），避免歧义。
- DP 初始化：空串/空模式的边界，尤其正则的 `*`。

## 练习串联（巩固路径）
- 签名分组 → 49（加大数据量感受计数签名优势）。
- 基础窗口 → 3（再做 438 找所有异位词，联动计数思维）。
- 中心扩展 → 647 → 5（加深“奇/偶中心”与扩展边界的熟练度）。
- 进阶窗口 → 76（把 `need/window/valid` 逻辑写顺）。
- 二维DP → 72（确认三操作转移的直觉）。
- 综合DP → 10（掌握 `*` 的“零次/多次”两分支）。

## 下一步
- 若要系统化，我可以把每题的“微模板+易错点卡片”补到各题目录下，并在本页维护索引，形成可复用的学习网络。

## 微模板与易错点索引（统一入口）
- 49 字母异位词分组：./哈希与排序/字母异位词分组/微模板与易错点卡片.md
- 3 无重复字符的最长子串：./滑动窗口/无重复字符的最长子串/微模板与易错点卡片.md
- 647 回文子串：./双指针-中心扩展/回文子串/微模板与易错点卡片.md
- 5 最长回文子串：./双指针-中心扩展/最长回文子串/微模板与易错点卡片.md
- 76 最小覆盖子串：./滑动窗口/最小覆盖子串/微模板与易错点卡片.md
- 72 编辑距离：./动态规划-字符串/编辑距离/微模板与易错点卡片.md
- 10 正则表达式匹配：./动态规划-字符串/正则表达式匹配/微模板与易错点卡片.md