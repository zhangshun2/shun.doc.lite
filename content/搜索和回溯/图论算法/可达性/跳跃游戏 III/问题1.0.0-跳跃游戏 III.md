# 问题1.0.0 跳跃游戏 III（LeetCode-1306）

- 难度：中等
- 链接：https://leetcode.cn/problems/jump-game-iii/

## 概述
给定数组 `arr` 与起点索引 `start`，从 `i` 可跳到 `i + arr[i]` 或 `i - arr[i]`。判断是否能到达任一值为 `0` 的索引。

## 问题特征与分析引导
- 可达性：典型 BFS/DFS；数组越界与访问标记。
- 早停：到达值为 `0` 时立即返回。

## 可能涉及的知识点
- BFS 队列或 DFS 递归
- 访问标记与边界判定