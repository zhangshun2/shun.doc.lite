# 问题1.0.0 寻找名人（LeetCode-277）

- 难度：中等
- 链接：https://leetcode.cn/problems/find-the-celebrity/

## 概述
在 `n` 人中寻找名人：名人被所有人认识，但他不认识任何人。调用 `knows(a,b)` 可判断 `a` 是否认识 `b`。

## 问题特征与分析引导
- 候选消除：两两比较，若 `a` 认识 `b`，则 `a` 不是名人；否则 `b` 不是名人。最终只剩一个候选。
- 验证：候选必须不认识任何人，且所有人都认识候选。

## 可能涉及的知识点
- 线性筛选候选人
- 两次验证遍历