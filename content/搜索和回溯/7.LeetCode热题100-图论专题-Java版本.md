# 7. LeetCode热题100-图论专题-Java版本

# LeetCode 热题 100 - 图论专题 (Java版本)
> 图论算法的系统性学习指南，涵盖图的遍历、拓扑排序和图搜索算法的Java实现
>

## 📊 专题概览
+ **总题目数量**: 3题
+ **简单题**: 0题 (0%)
+ **中等题**: 3题 (100%)
+ **困难题**: 0题 (0%)

---

## 🔗 图论分类体系
### 1. 🌊 图遍历算法 (Graph Traversal) - 1题
#### 🏝️ 连通分量问题
+ ***� 200-岛屿数量**

**Java DFS模板**:

```java
public int numIslands(char[][] grid) {
    if (grid == null || grid.length == 0) return 0;
    
    int m = grid.length, n = grid[0].length;
    int count = 0;
    
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (grid[i][j] == '1') {
                dfs(grid, i, j);
                count++;
            }
        }
    }
    return count;
}

private void dfs(char[][] grid, int i, int j) {
    int m = grid.length, n = grid[0].length;
    
    // 边界检查和终止条件
    if (i < 0 || i >= m || j < 0 || j >= n || grid[i][j] != '1') {
        return;
    }
    
    // 标记为已访问
    grid[i][j] = '0';
    
    // 遍历四个方向
    int[][] directions = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};
    for (int[] dir : directions) {
        dfs(grid, i + dir[0], j + dir[1]);
    }
}
```

**Java BFS模板**:

```java
public int numIslandsBFS(char[][] grid) {
    if (grid == null || grid.length == 0) return 0;
    
    int m = grid.length, n = grid[0].length;
    int count = 0;
    
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (grid[i][j] == '1') {
                bfs(grid, i, j);
                count++;
            }
        }
    }
    return count;
}

private void bfs(char[][] grid, int startI, int startJ) {
    int m = grid.length, n = grid[0].length;
    Queue<int[]> queue = new LinkedList<>();
    
    queue.offer(new int[]{startI, startJ});
    grid[startI][startJ] = '0';
    
    int[][] directions = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};
    
    while (!queue.isEmpty()) {
        int[] curr = queue.poll();
        int x = curr[0], y = curr[1];
        
        for (int[] dir : directions) {
            int nx = x + dir[0], ny = y + dir[1];
            
            if (nx >= 0 && nx < m && ny >= 0 && ny < n && grid[nx][ny] == '1') {
                grid[nx][ny] = '0';
                queue.offer(new int[]{nx, ny});
            }
        }
    }
}
```

**扩展应用**:

    - **标签**: DFS/BFS、连通分量、二维图遍历
    - **算法类型**: 深度优先搜索 (DFS) / 广度优先搜索 (BFS)
    - **核心思想**: 遍历所有连通的陆地，统计连通分量个数
    - **时间复杂度**: O(m×n)
    - **空间复杂度**: O(m×n) - 递归栈空间
    - 岛屿的最大面积
    - 岛屿的周长
    - 被围绕的区域
    - 太平洋大西洋水流问题

### 2. 🎯 拓扑排序 (Topological Sort) - 1题
#### 📚 依赖关系处理
+ ***� 207-课程表**

**Java Kahn算法模板**:

```java
public boolean canFinish(int numCourses, int[][] prerequisites) {
    // 构建图和入度数组
    List<List<Integer>> graph = new ArrayList<>();
    int[] indegree = new int[numCourses];
    
    // 初始化图
    for (int i = 0; i < numCourses; i++) {
        graph.add(new ArrayList<>());
    }
    
    // 构建图和计算入度
    for (int[] prereq : prerequisites) {
        int course = prereq[0];
        int pre = prereq[1];
        graph.get(pre).add(course);
        indegree[course]++;
    }
    
    // BFS处理入度为0的节点
    Queue<Integer> queue = new LinkedList<>();
    for (int i = 0; i < numCourses; i++) {
        if (indegree[i] == 0) {
            queue.offer(i);
        }
    }
    
    int count = 0;
    while (!queue.isEmpty()) {
        int node = queue.poll();
        count++;
        
        for (int neighbor : graph.get(node)) {
            indegree[neighbor]--;
            if (indegree[neighbor] == 0) {
                queue.offer(neighbor);
            }
        }
    }
    
    return count == numCourses;
}
```

**Java DFS检测环模板**:

```java
public boolean canFinishDFS(int numCourses, int[][] prerequisites) {
    List<List<Integer>> graph = new ArrayList<>();
    
    // 初始化图
    for (int i = 0; i < numCourses; i++) {
        graph.add(new ArrayList<>());
    }
    
    // 构建图
    for (int[] prereq : prerequisites) {
        int course = prereq[0];
        int pre = prereq[1];
        graph.get(pre).add(course);
    }
    
    // 0: 未访问, 1: 正在访问, 2: 已完成
    int[] state = new int[numCourses];
    
    for (int i = 0; i < numCourses; i++) {
        if (!dfs(graph, i, state)) {
            return false;
        }
    }
    return true;
}

private boolean dfs(List<List<Integer>> graph, int node, int[] state) {
    if (state[node] == 1) {  // 发现环
        return false;
    }
    if (state[node] == 2) {  // 已访问
        return true;
    }
    
    state[node] = 1;  // 标记为正在访问
    
    for (int neighbor : graph.get(node)) {
        if (!dfs(graph, neighbor, state)) {
            return false;
        }
    }
    
    state[node] = 2;  // 标记为已完成
    return true;
}
```

**扩展应用**:

    - **标签**: 拓扑排序、有向图、环检测
    - **算法类型**: Kahn算法 / DFS检测环
    - **核心思想**: 检测有向图中是否存在环
    - **时间复杂度**: O(V + E)
    - **空间复杂度**: O(V + E)
    - 课程表II (输出拓扑排序序列)
    - 外星词典
    - 任务调度
    - 编译顺序

### 3. 🔍 带权图搜索 (Weighted Graph Search) - 1题
#### ➗ 路径查询问题
+ ***� 399-除法求值**

**Java DFS搜索模板**:

```java
public double[] calcEquation(List<List<String>> equations, double[] values, List<List<String>> queries) {
    // 构建带权图
    Map<String, Map<String, Double>> graph = new HashMap<>();
    
    for (int i = 0; i < equations.size(); i++) {
        String a = equations.get(i).get(0);
        String b = equations.get(i).get(1);
        double value = values[i];
        
        graph.putIfAbsent(a, new HashMap<>());
        graph.putIfAbsent(b, new HashMap<>());
        
        graph.get(a).put(b, value);
        graph.get(b).put(a, 1.0 / value);
    }
    
    double[] results = new double[queries.size()];
    
    for (int i = 0; i < queries.size(); i++) {
        String start = queries.get(i).get(0);
        String end = queries.get(i).get(1);
        results[i] = dfs(graph, start, end, new HashSet<>());
    }
    
    return results;
}

private double dfs(Map<String, Map<String, Double>> graph, String start, String end, Set<String> visited) {
    if (!graph.containsKey(start) || !graph.containsKey(end)) {
        return -1.0;
    }
    if (start.equals(end)) {
        return 1.0;
    }
    
    visited.add(start);
    
    for (Map.Entry<String, Double> neighbor : graph.get(start).entrySet()) {
        String next = neighbor.getKey();
        double weight = neighbor.getValue();
        
        if (!visited.contains(next)) {
            double result = dfs(graph, next, end, visited);
            if (result != -1.0) {
                visited.remove(start);
                return weight * result;
            }
        }
    }
    
    visited.remove(start);
    return -1.0;
}
```

**Java 并查集模板**:

```java
class UnionFind {
    private Map<String, String> parent;
    private Map<String, Double> weight;
    
    public UnionFind() {
        parent = new HashMap<>();
        weight = new HashMap<>();
    }
    
    public String find(String x) {
        if (!parent.containsKey(x)) {
            parent.put(x, x);
            weight.put(x, 1.0);
            return x;
        }
        
        if (!parent.get(x).equals(x)) {
            String origin = parent.get(x);
            parent.put(x, find(origin));
            weight.put(x, weight.get(x) * weight.get(origin));
        }
        return parent.get(x);
    }
    
    public void union(String x, String y, double value) {
        String rootX = find(x);
        String rootY = find(y);
        
        if (!rootX.equals(rootY)) {
            parent.put(rootX, rootY);
            weight.put(rootX, weight.get(y) * value / weight.get(x));
        }
    }
    
    public double query(String x, String y) {
        if (!parent.containsKey(x) || !parent.containsKey(y)) {
            return -1.0;
        }
        
        String rootX = find(x);
        String rootY = find(y);
        
        if (!rootX.equals(rootY)) {
            return -1.0;
        }
        
        return weight.get(x) / weight.get(y);
    }
}

public double[] calcEquationUnionFind(List<List<String>> equations, double[] values, List<List<String>> queries) {
    UnionFind uf = new UnionFind();
    
    // 构建并查集
    for (int i = 0; i < equations.size(); i++) {
        String a = equations.get(i).get(0);
        String b = equations.get(i).get(1);
        uf.union(a, b, values[i]);
    }
    
    double[] results = new double[queries.size()];
    
    for (int i = 0; i < queries.size(); i++) {
        String a = queries.get(i).get(0);
        String b = queries.get(i).get(1);
        results[i] = uf.query(a, b);
    }
    
    return results;
}
```

**扩展应用**:

    - **标签**: 带权图、路径查询、并查集
    - **算法类型**: DFS路径搜索 / 并查集 / Floyd算法
    - **核心思想**: 构建带权图，查询两点间的路径权重
    - **时间复杂度**: O(E + Q×V) - E为边数，Q为查询数
    - **空间复杂度**: O(V + E)
    - 账户合并
    - 等式方程的可满足性
    - 冗余连接
    - 最小生成树

---

## 🛠️ Java图论算法模板
### 📋 图的表示方法
#### 🔗 邻接表 (推荐)
```java
// 无权图
List<List<Integer>> graph = new ArrayList<>();
for (int i = 0; i < n; i++) {
    graph.add(new ArrayList<>());
}
graph.get(u).add(v);

// 带权图 - 使用内部类
class Edge {
    int to;
    int weight;
    
    Edge(int to, int weight) {
        this.to = to;
        this.weight = weight;
    }
}

List<List<Edge>> graph = new ArrayList<>();
for (int i = 0; i < n; i++) {
    graph.add(new ArrayList<>());
}
graph.get(u).add(new Edge(v, weight));

// 字典表示
Map<Integer, List<Integer>> graph = new HashMap<>();
graph.putIfAbsent(u, new ArrayList<>());
graph.get(u).add(v);
```

#### 📊 邻接矩阵
```java
// 适用于稠密图
int[][] graph = new int[n][n];
graph[u][v] = 1;  // 或权重

// 布尔矩阵
boolean[][] graph = new boolean[n][n];
graph[u][v] = true;
```

### 🌊 图遍历模板
#### 🔍 深度优先搜索 (DFS)
```java
public void dfs(List<List<Integer>> graph, int start, boolean[] visited) {
    visited[start] = true;
    // 处理当前节点
    
    for (int neighbor : graph.get(start)) {
        if (!visited[neighbor]) {
            dfs(graph, neighbor, visited);
        }
    }
}

// 使用Set的版本
public void dfsWithSet(Map<Integer, List<Integer>> graph, int start, Set<Integer> visited) {
    visited.add(start);
    // 处理当前节点
    
    if (graph.containsKey(start)) {
        for (int neighbor : graph.get(start)) {
            if (!visited.contains(neighbor)) {
                dfsWithSet(graph, neighbor, visited);
            }
        }
    }
}
```

#### 🌊 广度优先搜索 (BFS)
```java
public void bfs(List<List<Integer>> graph, int start) {
    boolean[] visited = new boolean[graph.size()];
    Queue<Integer> queue = new LinkedList<>();
    
    visited[start] = true;
    queue.offer(start);
    
    while (!queue.isEmpty()) {
        int node = queue.poll();
        // 处理当前节点
        
        for (int neighbor : graph.get(node)) {
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                queue.offer(neighbor);
            }
        }
    }
}

// 使用Set的版本
public void bfsWithSet(Map<Integer, List<Integer>> graph, int start) {
    Set<Integer> visited = new HashSet<>();
    Queue<Integer> queue = new LinkedList<>();
    
    visited.add(start);
    queue.offer(start);
    
    while (!queue.isEmpty()) {
        int node = queue.poll();
        // 处理当前节点
        
        if (graph.containsKey(node)) {
            for (int neighbor : graph.get(node)) {
                if (!visited.contains(neighbor)) {
                    visited.add(neighbor);
                    queue.offer(neighbor);
                }
            }
        }
    }
}
```

### 🎯 拓扑排序模板
#### 📊 Kahn算法 (BFS)
```java
public List<Integer> topologicalSort(List<List<Integer>> graph, int n) {
    int[] indegree = new int[n];
    
    // 计算入度
    for (int u = 0; u < n; u++) {
        for (int v : graph.get(u)) {
            indegree[v]++;
        }
    }
    
    Queue<Integer> queue = new LinkedList<>();
    for (int i = 0; i < n; i++) {
        if (indegree[i] == 0) {
            queue.offer(i);
        }
    }
    
    List<Integer> result = new ArrayList<>();
    
    while (!queue.isEmpty()) {
        int node = queue.poll();
        result.add(node);
        
        for (int neighbor : graph.get(node)) {
            indegree[neighbor]--;
            if (indegree[neighbor] == 0) {
                queue.offer(neighbor);
            }
        }
    }
    
    return result.size() == n ? result : new ArrayList<>();
}
```

#### 🔍 DFS拓扑排序
```java
public List<Integer> topologicalSortDFS(List<List<Integer>> graph, int n) {
    final int WHITE = 0, GRAY = 1, BLACK = 2;
    int[] color = new int[n];
    List<Integer> result = new ArrayList<>();
    
    for (int i = 0; i < n; i++) {
        if (color[i] == WHITE) {
            if (!dfs(graph, i, color, result)) {
                return new ArrayList<>();  // 有环
            }
        }
    }
    
    Collections.reverse(result);
    return result;
}

private boolean dfs(List<List<Integer>> graph, int node, int[] color, List<Integer> result) {
    final int WHITE = 0, GRAY = 1, BLACK = 2;
    
    if (color[node] == GRAY) {
        return false;  // 发现环
    }
    if (color[node] == BLACK) {
        return true;
    }
    
    color[node] = GRAY;
    
    for (int neighbor : graph.get(node)) {
        if (!dfs(graph, neighbor, color, result)) {
            return false;
        }
    }
    
    color[node] = BLACK;
    result.add(node);
    return true;
}
```

---

## 📚 学习路径建议
### 🎯 阶段一：图遍历基础
1. ***� 200-岛屿数量**
    - 掌握DFS和BFS的基本模板
    - 理解二维图的遍历方法
    - 学会处理连通分量问题

### 🎯 阶段二：有向图算法
2. ***� 207-课程表**
    - 理解拓扑排序的概念和应用
    - 掌握Kahn算法和DFS检测环
    - 学会处理依赖关系问题

### 🎯 阶段三：带权图搜索
3. ***� 399-除法求值**
    - 理解带权图的构建和搜索
    - 掌握并查集的高级应用
    - 学会处理路径查询问题

---

## 💡 Java解题技巧总结
### 🔍 图遍历技巧
+ **选择DFS还是BFS**: 
    - DFS: 路径问题、连通性检测
    - BFS: 最短路径、层次遍历
+ **避免重复访问**: 使用boolean[]数组或Set集合
+ **方向数组**: 二维图遍历的标准技巧

### 🎯 拓扑排序技巧
+ **环检测**: 拓扑排序的核心应用
+ **入度统计**: Kahn算法的关键
+ **三色标记**: DFS检测环的经典方法

### 🔗 图构建技巧
+ **邻接表vs邻接矩阵**: 根据图的稠密度选择
+ **双向边**: 无向图需要添加双向边
+ **权重处理**: 带权图的边权存储

### 🛡️ Java编程注意事项
+ **集合初始化**: 正确初始化List和Map
+ **边界检查**: 防止数组越界和空指针异常
+ **内存管理**: 合理使用数据结构避免内存溢出
+ **类型安全**: 使用泛型确保类型安全

---

## 🔗 相关算法扩展
### 🌟 最短路径算法
+ **Dijkstra算法**: 单源最短路径
+ **Floyd算法**: 多源最短路径
+ **Bellman-Ford算法**: 处理负权边

### 🌲 最小生成树
+ **Kruskal算法**: 基于边的贪心算法
+ **Prim算法**: 基于点的贪心算法

### 🔄 强连通分量
+ **Tarjan算法**: 基于DFS的强连通分量
+ **Kosaraju算法**: 两次DFS求强连通分量

---

## 📈 进阶学习建议
### 🎯 掌握程度要求
1. **基础要求**: 能够熟练实现DFS和BFS
2. **进阶要求**: 理解拓扑排序的原理和应用
3. **高级要求**: 掌握并查集和带权图搜索

### 🔗 相关专题链接
+ **树的遍历**: 图遍历的特殊情况
+ **动态规划**: 图上的DP问题
+ **贪心算法**: 最短路径和最小生成树
+ **并查集**: 图的连通性问题

---

_更新时间：2024年12月_

> 🔗 图论是算法中的重要分支，虽然热题100中图论题目不多，但每一题都是经典，建议深入理解算法原理和模板应用！Java版本提供了完整的代码实现和优化技巧，助你在面试中脱颖而出！
>



> 更新: 2025-08-25 19:01:31  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/9ffd936b19502e76799a7cfb48cb162a>