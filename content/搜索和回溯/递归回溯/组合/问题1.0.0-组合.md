# 问题1.0.0 组合（LeetCode-77）

- 难度：中等
- 链接：https://leetcode.cn/problems/combinations/

## 概述
给定两个整数 `n` 和 `k`，返回 `[1..n]` 中所有可能的 `k` 个数的组合。

## 问题特征与分析引导
- 生成规模：`C(n,k)`；指数增长，典型回溯枚举。
- 剪枝策略：剩余可选数量不足则提前停止（`(n - i + 1) < (k - path.size())`）。

## 可能涉及的知识点
- 回溯模板：按递增选择，控制 `start` 起点
- 组合枚举与剪枝