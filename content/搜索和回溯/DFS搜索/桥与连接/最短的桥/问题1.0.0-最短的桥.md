# 问题1.0.0 最短的桥（LeetCode-934）

- 难度：中等
- 链接：https://leetcode.cn/problems/shortest-bridge/

## 概述
给定一个仅包含两个岛的 `0/1` 网格（`1` 为陆地），通过把若干 `0` 变为 `1` 连通两岛，求需要的最少翻转次数（即最短桥长度）。

## 问题特征与分析引导
- 两阶段：先找到并标记一个岛；再从该岛做多源 BFS 扩张，首次接触另一个岛时步数即答案。
- 细节：DFS 标记 + BFS 层次，四方向。

## 可能涉及的知识点
- DFS 找连通块与入队
- 多源 BFS 最短层数