# 问题1.0.0 单词接龙（LeetCode-127）

- 难度：中等
- 链接：https://leetcode.cn/problems/word-ladder/

## 概述
给定起始单词 `beginWord`、目标单词 `endWord` 和字典 `wordList`，每次只能改变一个字母且目标必须在字典中，返回最短转换序列长度（包括起点和终点）。

## 问题特征与分析引导
- 状态图：每个词与所有“一字之差”的词相连。
- BFS：层序即最少步数；访问标记防止重复与回环。

## 可能涉及的知识点
- 邻居生成（枚举位置与字母）
- BFS 队列与步数统计