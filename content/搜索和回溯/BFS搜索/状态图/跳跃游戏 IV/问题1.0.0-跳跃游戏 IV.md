# 问题1.0.0 跳跃游戏 IV（LeetCode-1345）

- 难度：困难
- 链接：https://leetcode.cn/problems/jump-game-iv/

## 概述
给定数组，可从索引 `i` 跳到 `i-1`、`i+1`，或跳到任意值与 `arr[i]` 相同的索引。求到达末尾的最少跳数。

## 问题特征与分析引导
- 状态图：按值建映射 `value -> indices`，同值索引连通。
- 剪枝：同值索引展开一次后应清空，避免重复膨胀。

## 可能涉及的知识点
- 值映射邻接生成
- BFS 层序与剪枝