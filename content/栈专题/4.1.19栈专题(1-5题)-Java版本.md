# 4.1.19 栈专题(1-5题)-Java版本

# 4.1.19 栈专题(1-5题) - Java版本
> 本文档包含LeetCode热题100中栈专题的第1-5道题目，提供基础解法和进阶优化版本
>

---

## 📊 题目概览
| 题号 | 题目 | 难度 | 标签 |
| --- | --- | --- | --- |
| 20 | 有效的括号 | 🟢 简单 | 栈, 字符串 |
| 155 | 最小栈 | 🟡 中等 | 栈, 设计 |
| 394 | 字符串解码 | 🟡 中等 | 栈, 递归, 字符串 |
| 739 | 每日温度 | 🟡 中等 | 栈, 数组, 单调栈 |
| 84 | 柱状图中最大的矩形 | 🔴 困难 | 栈, 数组, 单调栈 |


---

## 🎯 栈的核心概念
### 🔑 栈的基本特性
1. **LIFO原则**：后进先出（Last In First Out）
2. **基本操作**：push（入栈）、pop（出栈）、peek/top（查看栈顶）
3. **应用场景**：函数调用、表达式求值、括号匹配、回溯算法

### 📝 栈的高级应用
1. **单调栈**：维护栈内元素的单调性
2. **辅助栈**：使用额外栈来维护特殊信息
3. **栈模拟递归**：将递归转换为迭代
4. **表达式处理**：中缀、后缀表达式的转换和计算

---

## 1️⃣ 有效的括号 (LeetCode 20)
### 📝 题目描述
给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。

有效字符串需满足：

1. 左括号必须用相同类型的右括号闭合。
2. 左括号必须以正确的顺序闭合。
3. 每个右括号都有一个对应的相同类型的左括号。

**示例 1：**

```plain
输入：s = "()"
输出：true
```

**示例 2：**

```plain
输入：s = "()[]{}"
输出：true
```

**示例 3：**

```plain
输入：s = "(]"
输出：false
```

### 🔧 基础解法：栈匹配
```java
class Solution {
    public boolean isValid(String s) {
        Stack<Character> stack = new Stack<>();
        
        for (char c : s.toCharArray()) {
            // 左括号入栈
            if (c == '(' || c == '[' || c == '{') {
                stack.push(c);
            } else {
                // 右括号时检查匹配
                if (stack.isEmpty()) {
                    return false;
                }
                
                char top = stack.pop();
                if ((c == ')' && top != '(') ||
                    (c == ']' && top != '[') ||
                    (c == '}' && top != '{')) {
                    return false;
                }
            }
        }
        
        return stack.isEmpty();
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 进阶解法：哈希表优化
```java
class Solution {
    public boolean isValid(String s) {
        if (s.length() % 2 == 1) {
            return false; // 奇数长度不可能有效
        }
        
        Map<Character, Character> pairs = new HashMap<Character, Character>() {{
            put(')', '(');
            put(']', '[');
            put('}', '{');
        }};
        
        Stack<Character> stack = new Stack<>();
        
        for (char c : s.toCharArray()) {
            if (pairs.containsKey(c)) {
                // 右括号
                if (stack.isEmpty() || !stack.pop().equals(pairs.get(c))) {
                    return false;
                }
            } else {
                // 左括号
                stack.push(c);
            }
        }
        
        return stack.isEmpty();
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 最优解法：数组模拟栈
```java
class Solution {
    public boolean isValid(String s) {
        int n = s.length();
        if (n % 2 == 1) {
            return false;
        }
        
        char[] stack = new char[n];
        int top = -1;
        
        for (char c : s.toCharArray()) {
            if (c == '(' || c == '[' || c == '{') {
                stack[++top] = c;
            } else {
                if (top == -1) {
                    return false;
                }
                
                char left = stack[top--];
                if ((c == ')' && left != '(') ||
                    (c == ']' && left != '[') ||
                    (c == '}' && left != '{')) {
                    return false;
                }
            }
        }
        
        return top == -1;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)，但常数因子更小

---

## 2️⃣ 最小栈 (LeetCode 155)
### 📝 题目描述
设计一个支持 `push` ，`pop` ，`top` 操作，并能在常数时间内检索到最小元素的栈。

实现 `MinStack` 类:

+ `MinStack()` 初始化堆栈对象。
+ `void push(int val)` 将元素val推入堆栈。
+ `void pop()` 删除堆栈顶部的元素。
+ `int top()` 获取堆栈顶部的元素。
+ `int getMin()` 获取堆栈中的最小元素。

**示例：**

```plain
输入：
["MinStack","push","push","push","getMin","pop","top","getMin"]
[[],[-2],[0],[-3],[],[],[],[]]

输出：
[null,null,null,null,-3,null,0,-2]

解释：
MinStack minStack = new MinStack();
minStack.push(-2);
minStack.push(0);
minStack.push(-3);
minStack.getMin(); // 返回 -3
minStack.pop();
minStack.top();    // 返回 0
minStack.getMin(); // 返回 -2
```

### 🔧 基础解法：辅助栈
```java
class MinStack {
    private Stack<Integer> stack;
    private Stack<Integer> minStack;
    
    public MinStack() {
        stack = new Stack<>();
        minStack = new Stack<>();
    }
    
    public void push(int val) {
        stack.push(val);
        
        // 维护最小值栈
        if (minStack.isEmpty() || val <= minStack.peek()) {
            minStack.push(val);
        }
    }
    
    public void pop() {
        int val = stack.pop();
        
        // 如果弹出的是最小值，同时弹出最小值栈
        if (val == minStack.peek()) {
            minStack.pop();
        }
    }
    
    public int top() {
        return stack.peek();
    }
    
    public int getMin() {
        return minStack.peek();
    }
}
```

**复杂度分析：**

+ 时间复杂度：所有操作都是O(1)
+ 空间复杂度：O(n)

### 🚀 进阶解法：单栈+差值
```java
class MinStack {
    private Stack<Long> stack;
    private long min;
    
    public MinStack() {
        stack = new Stack<>();
    }
    
    public void push(int val) {
        if (stack.isEmpty()) {
            stack.push(0L);
            min = val;
        } else {
            // 存储与当前最小值的差值
            stack.push((long) val - min);
            if (val < min) {
                min = val;
            }
        }
    }
    
    public void pop() {
        if (stack.isEmpty()) {
            return;
        }
        
        long pop = stack.pop();
        
        // 如果差值小于0，说明弹出的是最小值
        if (pop < 0) {
            min = min - pop; // 恢复之前的最小值
        }
    }
    
    public int top() {
        long top = stack.peek();
        
        if (top > 0) {
            return (int) (top + min);
        } else {
            return (int) min;
        }
    }
    
    public int getMin() {
        return (int) min;
    }
}
```

**复杂度分析：**

+ 时间复杂度：所有操作都是O(1)
+ 空间复杂度：O(n)，但空间使用更少

### 🚀 最优解法：链表实现
```java
class MinStack {
    private Node head;
    
    private class Node {
        int val;
        int min;
        Node next;
        
        Node(int val, int min, Node next) {
            this.val = val;
            this.min = min;
            this.next = next;
        }
    }
    
    public MinStack() {
        // 头节点为空
    }
    
    public void push(int val) {
        if (head == null) {
            head = new Node(val, val, null);
        } else {
            head = new Node(val, Math.min(val, head.min), head);
        }
    }
    
    public void pop() {
        head = head.next;
    }
    
    public int top() {
        return head.val;
    }
    
    public int getMin() {
        return head.min;
    }
}
```

**复杂度分析：**

+ 时间复杂度：所有操作都是O(1)
+ 空间复杂度：O(n)

---

## 3️⃣ 字符串解码 (LeetCode 394)
### 📝 题目描述
给定一个经过编码的字符串，返回它解码后的字符串。

编码规则为: `k[encoded_string]`，表示其中方括号内部的 `encoded_string` 正好重复 `k` 次。注意 `k` 保证为正整数。

你可以认为输入字符串总是有效的；输入字符串中没有额外的空格，且输入的方括号总是符合格式要求的。

此外，你可以认为原始数据不包含数字，所有的数字只表示重复的次数 `k` ，例如不会出现像 `3a` 或 `2[4]` 的输入。

**示例 1：**

```plain
输入：s = "3[a]2[bc]"
输出："aaabcbc"
```

**示例 2：**

```plain
输入：s = "2[abc]3[cd]ef"
输出："abcabccdcdcdef"
```

**示例 3：**

```plain
输入：s = "abc3[cd]xyz"
输出："abccdcdcdxyz"
```

### 🔧 基础解法：双栈
```java
class Solution {
    public String decodeString(String s) {
        Stack<Integer> countStack = new Stack<>();
        Stack<StringBuilder> stringStack = new Stack<>();
        StringBuilder currentString = new StringBuilder();
        int k = 0;
        
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                k = k * 10 + (c - '0');
            } else if (c == '[') {
                // 保存当前状态
                countStack.push(k);
                stringStack.push(currentString);
                
                // 重置
                currentString = new StringBuilder();
                k = 0;
            } else if (c == ']') {
                // 恢复状态并构建字符串
                StringBuilder decodedString = stringStack.pop();
                int currentK = countStack.pop();
                
                for (int i = 0; i < currentK; i++) {
                    decodedString.append(currentString);
                }
                
                currentString = decodedString;
            } else {
                currentString.append(c);
            }
        }
        
        return currentString.toString();
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)，n是解码后字符串的长度
+ 空间复杂度：O(n)

### 🚀 进阶解法：递归
```java
class Solution {
    private int index = 0;
    
    public String decodeString(String s) {
        return decode(s);
    }
    
    private String decode(String s) {
        StringBuilder result = new StringBuilder();
        int num = 0;
        
        while (index < s.length()) {
            char c = s.charAt(index);
            
            if (Character.isDigit(c)) {
                num = num * 10 + (c - '0');
            } else if (c == '[') {
                index++; // 跳过 '['
                String sub = decode(s); // 递归解码
                
                // 重复 num 次
                for (int i = 0; i < num; i++) {
                    result.append(sub);
                }
                
                num = 0; // 重置
            } else if (c == ']') {
                break; // 结束当前层递归
            } else {
                result.append(c);
            }
            
            index++;
        }
        
        return result.toString();
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)，递归调用栈

### 🚀 最优解法：优化的栈
```java
class Solution {
    public String decodeString(String s) {
        Stack<String> stringStack = new Stack<>();
        Stack<Integer> countStack = new Stack<>();
        String currentString = "";
        int k = 0;
        
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                k = k * 10 + (c - '0');
            } else if (c == '[') {
                countStack.push(k);
                stringStack.push(currentString);
                currentString = "";
                k = 0;
            } else if (c == ']') {
                StringBuilder temp = new StringBuilder(stringStack.pop());
                int repeatTimes = countStack.pop();
                
                for (int i = 0; i < repeatTimes; i++) {
                    temp.append(currentString);
                }
                
                currentString = temp.toString();
            } else {
                currentString += c;
            }
        }
        
        return currentString;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

---

## 4️⃣ 每日温度 (LeetCode 739)
### 📝 题目描述
给定一个整数数组 `temperatures` ，表示每天的温度，返回一个数组 `answer` ，其中 `answer[i]` 是指对于第 `i` 天，下一个更高温度出现在几天后。如果气温在这之后都不会升高，请在该位置用 `0` 来代替。

**示例 1:**

```plain
输入: temperatures = [73,74,75,71,69,72,76,73]
输出: [1,1,4,2,1,1,0,0]
```

**示例 2:**

```plain
输入: temperatures = [30,40,50,60]
输出: [1,1,1,0]
```

**示例 3:**

```plain
输入: temperatures = [30,60,90]
输出: [1,1,0]
```

### 🔧 基础解法：暴力搜索
```java
class Solution {
    public int[] dailyTemperatures(int[] temperatures) {
        int n = temperatures.length;
        int[] answer = new int[n];
        
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                if (temperatures[j] > temperatures[i]) {
                    answer[i] = j - i;
                    break;
                }
            }
        }
        
        return answer;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n²)
+ 空间复杂度：O(1)

### 🚀 进阶解法：单调栈
```java
class Solution {
    public int[] dailyTemperatures(int[] temperatures) {
        int n = temperatures.length;
        int[] answer = new int[n];
        Stack<Integer> stack = new Stack<>();
        
        for (int i = 0; i < n; i++) {
            // 当前温度比栈顶索引对应的温度高
            while (!stack.isEmpty() && temperatures[i] > temperatures[stack.peek()]) {
                int prevIndex = stack.pop();
                answer[prevIndex] = i - prevIndex;
            }
            
            stack.push(i);
        }
        
        return answer;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 最优解法：数组模拟栈
```java
class Solution {
    public int[] dailyTemperatures(int[] temperatures) {
        int n = temperatures.length;
        int[] answer = new int[n];
        int[] stack = new int[n];
        int top = -1;
        
        for (int i = 0; i < n; i++) {
            while (top >= 0 && temperatures[i] > temperatures[stack[top]]) {
                int prevIndex = stack[top--];
                answer[prevIndex] = i - prevIndex;
            }
            
            stack[++top] = i;
        }
        
        return answer;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)，但常数因子更小

---

## 5️⃣ 柱状图中最大的矩形 (LeetCode 84)
### 📝 题目描述
给定 _n_ 个非负整数，用来表示柱状图中各个柱子的高度。每个柱子彼此相邻，且宽度为 1 。

求在该柱状图中，能够勾勒出来的矩形的最大面积。

**示例 1:**

```plain
输入：heights = [2,1,5,6,2,3]
输出：10
解释：最大的矩形为图中红色区域，面积为 10
```

**示例 2:**

```plain
输入：heights = [2,4]
输出：4
```

### 🔧 基础解法：暴力搜索
```java
class Solution {
    public int largestRectangleArea(int[] heights) {
        int n = heights.length;
        int maxArea = 0;
        
        for (int i = 0; i < n; i++) {
            int minHeight = heights[i];
            
            for (int j = i; j < n; j++) {
                minHeight = Math.min(minHeight, heights[j]);
                int width = j - i + 1;
                maxArea = Math.max(maxArea, minHeight * width);
            }
        }
        
        return maxArea;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n²)
+ 空间复杂度：O(1)

### 🚀 进阶解法：单调栈
```java
class Solution {
    public int largestRectangleArea(int[] heights) {
        Stack<Integer> stack = new Stack<>();
        int maxArea = 0;
        int n = heights.length;
        
        for (int i = 0; i <= n; i++) {
            int currentHeight = (i == n) ? 0 : heights[i];
            
            while (!stack.isEmpty() && currentHeight < heights[stack.peek()]) {
                int height = heights[stack.pop()];
                int width = stack.isEmpty() ? i : i - stack.peek() - 1;
                maxArea = Math.max(maxArea, height * width);
            }
            
            stack.push(i);
        }
        
        return maxArea;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

### 🚀 最优解法：优化的单调栈
```java
class Solution {
    public int largestRectangleArea(int[] heights) {
        int n = heights.length;
        int[] stack = new int[n + 1];
        int top = -1;
        int maxArea = 0;
        
        for (int i = 0; i <= n; i++) {
            int currentHeight = (i == n) ? 0 : heights[i];
            
            while (top >= 0 && currentHeight < heights[stack[top]]) {
                int height = heights[stack[top--]];
                int width = (top == -1) ? i : i - stack[top] - 1;
                maxArea = Math.max(maxArea, height * width);
            }
            
            stack[++top] = i;
        }
        
        return maxArea;
    }
}
```

**复杂度分析：**

+ 时间复杂度：O(n)
+ 空间复杂度：O(n)

---

## 📚 总结
### 🎯 核心知识点
1. **栈的基本应用**：括号匹配、表达式求值
2. **辅助栈技巧**：维护额外信息（如最小值）
3. **单调栈**：解决"下一个更大/更小元素"类问题
4. **栈与递归**：栈可以模拟递归过程
5. **空间优化**：使用数组模拟栈减少开销

### 💡 解题模式
+ **括号匹配**：使用栈进行配对检查
+ **设计题**：合理使用辅助数据结构
+ **字符串处理**：栈处理嵌套结构
+ **单调栈应用**：寻找下一个更大/更小元素
+ **面积计算**：单调栈计算最大矩形面积

### 🔄 刷题建议
1. **掌握基本操作**：熟练使用栈的push、pop、peek操作
2. **理解单调栈**：掌握单调栈的构建和应用
3. **练习设计题**：学会使用辅助数据结构
4. **优化空间使用**：考虑使用数组模拟栈
5. **理解应用场景**：知道何时使用栈解决问题

---

**上一篇：** [4.1.18 搜索与回溯专题(11题) - Java版本](./4.1.18%20搜索与回溯专题(11题)-Java版本.md)  
**下一篇：** [4.1.20 栈专题(6-8题) - Java版本](./4.1.20%20栈专题(6-8题)-Java版本.md)



> 更新: 2025-08-25 19:00:35  
> 原文: <https://www.yuque.com/zhangshun-xxqvr/vg2bou/026c4859a8cf618c134a202dd2ffd28a>