# LeetCode-33 搜索旋转排序数组（深度提问）

## 我现在应该能回答
- 每次二分时，如何判断左半边还是右半边是有序的
- 为什么有序的一边可以用区间判断来缩小搜索范围
- 为什么复杂度是 O(log n)

## 我还可以深入问
- 如果数组里允许重复元素（LeetCode-81），判断逻辑为什么会变复杂
- 如何用图把旋转数组拆成两段有序区间
- 如何处理边界，比如只有 1 个元素或者完全没旋转

## 易错点清单
- 判断有序一侧的条件：`nums[left] <= nums[mid]`
- 区间判断要用闭开/闭闭一致，避免漏掉 target
- `while (left <= right)` 里 mid 要用防溢出的写法

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

