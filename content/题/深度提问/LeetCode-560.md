# LeetCode-560 和为 K 的子数组（深度提问）

## 我现在应该能回答
- 为什么前缀和能把“区间和”转换成“差值”
- 为什么要先统计 `ans += cnt.get(sum-k)`，再把当前 sum 放进 map

## 我还可以深入问
- 如果数组全是非负数，能不能用双指针/滑动窗口做，为什么这题不行
- `k` 很大或很小（负数）时，前缀和方法为什么仍然成立
- 如果要返回所有区间下标对，要怎么改

## 易错点清单
- 初始必须 `cnt.put(0,1)`，否则漏掉从 0 开头的区间
- 先查再放，顺序反了会把长度为 0 的区间算进去

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

