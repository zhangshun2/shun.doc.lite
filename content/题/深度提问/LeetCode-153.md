# LeetCode-153 寻找旋转排序数组中的最小值（深度提问）

## 我现在应该能回答
- 为什么比较 `nums[m]` 和 `nums[r]` 就能判断最小值在哪边
- 为什么循环条件用 `l < r`，而不是 `l <= r`

## 我还可以深入问
- 如果允许重复元素（LeetCode-154），为什么就不一定是 O(log n)
- 这题能不能用 `nums[l]` 来比较，写法会有什么不同
- 如何直观理解“旋转数组”被分成两段递增区间

## 易错点清单
- 这题是“元素不重复”的版本，判断条件可以用 `>`
- 返回的是 `nums[l]`，不是 `l`

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

