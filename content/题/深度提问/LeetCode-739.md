# LeetCode-739 每日温度（深度提问）

## 我现在应该能回答
- 为什么这是“下一个更大元素”的模板题
- 栈里为什么存下标而不是存温度
- 为什么整体是 O(n)，不会退化成 O(n^2)

## 我还可以深入问
- 倒序跳跃法怎么写，和单调栈相比有何优缺点
- 如果要找“下一个更小元素”，模板怎么改
- 如果温度范围更大（不是 30~100），是否影响单调栈方案

## 易错点清单
- 弹栈后更新答案：`answer[prev] = i - prev`
- 比较用 `>`，不是 `>=`（题目要求更高温度）
- 最后栈里剩下的都填 0（默认值即可）

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

