# LeetCode-39 组合总和（深度提问）

## 我现在应该能回答
- 为什么这里适合用“回溯”，而不是暴力枚举所有子集
- 为什么递归参数里要有 `start`，它解决了什么重复问题

## 我还可以深入问
- 如果 `candidates` 里允许有重复数字，怎么避免重复组合
- 如果每个数字只能用一次（变成 LeetCode-40），递归怎么改
- 为什么排序 + `x > remain` 的剪枝是安全的

## 易错点清单
- 组合不看顺序：`[2,3,2]` 和 `[2,2,3]` 是同一个
- “能重复使用”时递归传 `i`，不是 `i + 1`

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

