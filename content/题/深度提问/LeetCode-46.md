# LeetCode-46 全排列（深度提问）

## 我现在应该能回答
- 为什么要用 `used[]`，不用会发生什么
- 为什么当 `idx == nums.length` 就是一种完整答案

## 我还可以深入问
- 如果 `nums` 里有重复元素（变成 LeetCode-47），如何去重
- 能不能用“交换法（原地交换）”写全排列，它和 used[] 有什么优缺点
- 全排列的时间复杂度为什么是 `O(n * n!)`

## 易错点清单
- 一定要回溯：递归返回后把 `used[i]` 还原
- 输出是值的排列，不是下标的排列

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

