# LeetCode-22 括号生成（深度提问）

## 我现在应该能回答
- 为什么这是回溯（递归枚举选择）问题
- 两个剪枝条件：`open < n` 和 `close < open` 为什么正确
- 为什么最终会有很多答案，不能用 DP 简单枚举字符串

## 我还可以深入问
- 结果数量为什么是第 n 个卡特兰数（不要求推公式，但要能解释直觉）
- 如果要按字典序输出，怎么保证顺序
- `StringBuilder` 的“增删”为什么比字符串拼接更省

## 易错点清单
- 只要 `close < open` 就可以放 `)`，不要写成 `close < n`
- 递归回溯时一定要把最后加的字符删掉
- 终止条件是 `open==n && close==n`，不是长度到了就直接收

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

