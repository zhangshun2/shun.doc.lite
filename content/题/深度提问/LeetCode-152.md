# LeetCode-152 乘积最大子数组（深度提问）

## 我现在应该能回答
- 为什么要同时维护“最大”和“最小”两个值
- 遇到 0 时，状态为什么会被“重置”

## 我还可以深入问
- 这题和最大子数组和（LeetCode-53）的 DP 思路哪里像、哪里不一样
- 如果只维护最大值，会在哪些例子上出错
- `nums[i]` 范围变大时，int 会不会溢出，该怎么处理

## 易错点清单
- 负数会导致最大最小互换，不能只看最大
- 初始化要用 `nums[0]`，不是 0

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

