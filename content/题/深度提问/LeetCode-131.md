# LeetCode-131 分割回文串（深度提问）

## 我现在应该能回答
- 为什么这是“枚举切法”的典型回溯题
- 为什么提前算 `pal[i][j]` 可以加速

## 我还可以深入问
- 不预处理 `pal`，每次现场判断回文会慢在哪里
- `pal` 的 DP 推导为什么要从右往左填（`i` 递减）
- 如果只要返回最少切割次数（变成 LeetCode-132），状态怎么设计

## 易错点清单
- `substring(start, end + 1)` 的 `end + 1` 很容易写错
- 回溯时要把当前段从 `path` 里移除

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

