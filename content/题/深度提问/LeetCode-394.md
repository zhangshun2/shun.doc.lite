# LeetCode-394 字符串解码（深度提问）

## 我现在应该能回答
- 为什么需要两个栈：一个存重复次数，一个存之前的字符串
- 为什么遇到 `[` 就要把当前字符串入栈并清空

## 我还可以深入问
- 如果输入可能不合法，要怎么做错误处理
- 如果解码后很长，`StringBuilder` 为什么更合适
- 能不能用递归（指针 + 返回）写，和栈写法怎么对应

## 易错点清单
- 多位数要用 `num = num * 10 + digit`
- `]` 结算时先弹次数，再弹上一个字符串

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

