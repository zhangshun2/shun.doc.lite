# LeetCode-78 子集（深度提问）

## 我现在应该能回答
- 为什么每加一个数字，子集数量会“翻倍”
- 为什么循环里要先保存 `size = ans.size()`

## 我还可以深入问
- 用回溯写子集和用迭代写子集，思维上有什么对应关系
- 如果 `nums` 里有重复元素（变成 LeetCode-90），怎么避免重复子集
- 子集总数是 `2^n`，这对时间复杂度意味着什么

## 易错点清单
- 不能一边遍历 `ans` 一边用 `ans.size()` 作为实时上界
- 复制子集时要新建 List，不能直接复用引用

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

