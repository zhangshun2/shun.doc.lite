# LeetCode-279 完全平方数（深度提问）

## 我现在应该能回答
- 为什么这是一个“完全背包/一维 DP”问题
- dp 转移 `dp[i] = min(dp[i], dp[i - k*k] + 1)` 的直觉是什么

## 我还可以深入问
- BFS（把每次减去一个平方数当作图的边）能不能做，复杂度如何
- 四平方和定理是什么，能不能更快判断答案是 1/2/3/4
- dp 里为什么要把初始值设为很大（INF）

## 易错点清单
- `k * k` 可能重复计算，但范围小无所谓
- `dp` 初始化别用 `Integer.MAX_VALUE` 直接加 1，会溢出

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

