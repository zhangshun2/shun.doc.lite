# LeetCode-208 实现 Trie (前缀树)（深度提问）

## 我现在应该能回答
- Trie 是用“共享前缀”来节省什么开销
- `end` 标记为什么必须存在（否则 search 会误判）

## 我还可以深入问
- 如果字符集更大（大小写、数字、Unicode），children 怎么设计
- Trie 和 HashSet 存单词的区别是什么（查询前缀是关键）
- 如何在 Trie 上实现删除单词，需要处理哪些节点回收

## 易错点清单
- `startsWith` 只要求走到节点存在，不要求 `end = true`
- 插入时最后要记得 `end = true`

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

