# LeetCode-32 最长有效括号（深度提问）

## 我现在应该能回答
- 为什么栈里存“下标”而不是存括号字符
- 哨兵 `-1` 的作用是什么
- 为什么在遇到无法匹配时要把当前位置当作新边界

## 我还可以深入问
- DP 解法怎么做，和栈解法各自的优缺点
- 如何输出“最长有效子串本身”，而不仅是长度
- 如果括号种类更多（比如 {}[]），还能用同样思路吗

## 易错点清单
- 遇到 `)` 时先 pop，再判断栈是否空
- 栈空时要 push 当前下标，作为新的边界
- 更新答案用 `i - stack.peek()`，不是 `i - stack.pop()`

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

