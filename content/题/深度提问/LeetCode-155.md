# LeetCode-155 最小栈（深度提问）

## 我现在应该能回答
- 为什么双栈能做到 getMin O(1)
- 为什么 push 时要用 `<=` 而不是 `<`
- pop 时为什么要同步弹出 minStack

## 我还可以深入问
- 单栈存对 `(val, curMin)` 的写法，空间与代码复杂度的取舍
- 如果要支持 getMax，也能用同样思路吗
- Java 里 `Stack` 和 `Deque` 的差别与选择理由

## 易错点清单
- 最小值重复出现时，minStack 必须存重复值
- pop 前要确保栈非空（按题意调用合法，但写代码时别漏）
- `int` 比较要注意不要用 `equals`（基本类型用 `==`）

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

