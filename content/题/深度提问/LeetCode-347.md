# LeetCode-347 前 K 个高频元素（深度提问）

## 我现在应该能回答
- 为什么用 k 大小的小顶堆就能得到前 k 高频
- 为什么堆里存 [num,count] 而不是只存 num
- 时间复杂度为什么是 O(n log k)

## 我还可以深入问
- 桶排序（按频率分桶）怎么写，复杂度能到 O(n) 吗
- 如果要求按频率从高到低输出并且频率相同按数值排序，怎么改
- k 很接近不同元素个数时，是否应该用别的方式

## 易错点清单
- k 是“前 k 高频”，不是“第 k 高频”
- 返回顺序不要求固定，不用额外排序浪费时间
- PriorityQueue 比较器要按 count 排

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

