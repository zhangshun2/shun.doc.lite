# LeetCode-51 N 皇后（深度提问）

## 我现在应该能回答
- 为什么每一行只放一个皇后，就不需要额外检查“行冲突”
- 两条对角线为什么可以用 `row + col` 和 `row - col + n` 表示

## 我还可以深入问
- 为什么对角线数组长度是 `2 * n`（不是 `n`）
- 返回具体棋盘时，为什么用 `queenCol[row]` 就够了
- n 变大时，剪枝还能怎么加强（例如位运算优化）

## 易错点清单
- 副对角线下标可能为负，需要加 `n` 做偏移
- 回溯时必须把列和对角线占用标记撤销

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

