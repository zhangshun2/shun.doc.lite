# LeetCode-98 验证二叉搜索树（深度提问）

## 我现在应该能回答
- 为什么“只比较父子节点大小”是不够的
- 为什么要把范围用 `long`，不用 `int`

## 我还可以深入问
- 用中序遍历判断 BST 的思路是什么，和上下界法有什么关系
- BST 允许重复值时（例如重复放右边），判断条件要怎么改
- 递归深度很深时，是否会栈溢出，能否改成迭代

## 易错点清单
- BST 比较是“严格”不等号：左 < 根 < 右
- 上下界要一路传递，不能只看一层

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

