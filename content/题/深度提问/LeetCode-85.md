# LeetCode-85 最大矩形（深度提问）

## 我现在应该能回答
- 为什么可以把每一行转换成“柱状图最大矩形”来做
- 单调栈里为什么存下标，而不是存高度

## 我还可以深入问
- 为什么结算宽度时用 `i - left - 1`
- `heights[st.peek()] > cur` 用 `>` 还是 `>=` 有什么区别
- 这个题和 LeetCode-84 的关系是什么

## 易错点清单
- 每行更新 `heights` 时遇到 `'0'` 必须清零
- 需要在最后加一个“高度为 0 的哨兵”把栈清空

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

