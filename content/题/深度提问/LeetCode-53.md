# LeetCode-53 最大子数组和（深度提问）

## 我现在应该能回答
- `cur = max(nums[i], cur + nums[i])` 这句为什么正确
- 为什么这个做法能保证连续子数组
- 为什么只用 O(1) 额外空间

## 我还可以深入问
- 如何返回“子数组的左右边界下标”
- 如果允许空子数组（和为 0），应该怎么改
- 分治解法怎么写，和 Kadane 有什么差别

## 易错点清单
- 初始值要用 nums[0]，不能用 0（全负数会错）
- `cur` 是“以 i 结尾”的最大和，不是全局最大
- 数组长度为 1 时也要正确

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

