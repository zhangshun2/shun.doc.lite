# LeetCode-438 找到字符串中所有字母异位词（深度提问）

## 我现在应该能回答
- 为什么固定窗口大小为 `p.length()` 就够了
- 为什么比较 26 个字母频次就能判断异位词

## 我还可以深入问
- 怎么把 `same()` 优化成 O(1) 更新（维护匹配的字母种类数）
- 如果字符集更大（不是 26 个小写字母），数据结构怎么换
- 这题和最小覆盖子串（LeetCode-76）的窗口思路哪里不同

## 易错点清单
- `i >= k` 时要移出 `i-k` 位置的字符
- 起点是 `i - k + 1`

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

