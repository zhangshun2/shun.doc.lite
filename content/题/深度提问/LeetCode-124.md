# LeetCode-124 二叉树中的最大路径和（深度提问）

## 我现在应该能回答
- 为什么“向父节点返回”的路径只能选左或右其中一边
- 为什么要对 `dfs` 的返回值做 `max(0, ...)`

## 我还可以深入问
- `ans` 记录的是哪一种路径：直上直下还是允许拐弯
- 如果所有节点都是负数，为什么答案仍然正确
- 这题和“最大子数组和（LeetCode-53）”有什么相似点

## 易错点清单
- 全局最大值必须用额外变量记录，不是 dfs 返回值本身
- 返回值是“向下单边”，更新答案是“左右都可用的拐点”

## 关联资料
- [模板索引.md](../../学习方法/模板索引.md)

